# Merch Fidelity Scorecard — the pixel-perfect ratchet

North star: **/merch is a PIXEL-PERFECT clone of merch.riotgames.com** at 1280px AND 390px.
This scorecard is the worklist for the **fidelity-engine** meta-loop (cron :53): each idle tick it
picks the next target (⬜ never-diffed first, then oldest-audited), runs a rigorous side-by-side
Playwright pixel-diff vs the real site, files measured `merch,status:ready` delta issues, and updates
the row. The build-loop (:11) then builds them. See `docs/loops/` for the loop briefs.

**Verdict:** ✅ pixel-match (within a few px) · ⚠️ deltas open (issue#s) · 🔁 re-diff queued · ⬜ never diffed
**Convergence:** when ALL rows ✅ and two full rotations file zero issues → CONVERGED (engine idles until the surface changes).

---

## PAGES (1280 + 390)

| Target | Real URL | Our route | Verdict | Last diff | Residual deltas |
|---|---|---|---|---|---|
| Homepage | `/` | `/merch` | ✅ | 2026-08-04 (confirm re-diff DRY) | SHIPPED (PR #664): franchise strip folded INTO hero as slide-control bar — 8 real logo SVGs, clip-path square-left tiles, 40px desktop gutter, tiles select slides. RESIDUALS FIXED (PR #671): bar `bottom-10` (40px art below), mobile `aspect-[195/272]` 544px + `pl-6 md:pl-10` (24px mobile gutter). **CONFIRM RE-DIFF DRY (fdiff-home2 2026-08-04):** #669 hero 540 vs real 535 (5px tol), #670 mobile hero 544 EXACT + gutter 24px EXACT, no 390 overflow, holistic sweep no new deltas. 1st clean diff-rotation. Prior residuals: announcement dismiss ✕ (pre-existing); nav FROZEN. |
| PDP | `/product/<h>` | `/merch/product/[handle]` | ✅ | 2026-08-04 (confirm re-diff DRY) | Gallery 1:1 (#649), price 28px ink-dark (#650), title 48/700/uppercase (#651) [PR #652], breadcrumb (#653/#654). BREADCRUMB FIXED (#672, PR #673): extracted `MerchBreadcrumbBar` (16px `--color-merch-ink` black current-crumb + grey parent crumbs, 40px padding, 60px desktop / 40px mobile, transparent) across all 4 pages. **CONFIRM RE-DIFF DRY (fdiff-pdp2 2026-08-04):** breadcrumb 16px/rgb(26,26,26)/40px pad/60-40 height EXACT at 1280+390; gallery 1:1 contain, title 48/700/uppercase, price 28px all MATCH; no 390 overflow. Minor: wishlist heart (low-pri); dead wrapper `--merch-bar-h` var (trivial cleanup). Out-of-scope: "PURCHASING DISABLED" + gold-vs-red add-to-cart (real live-commerce state) |
| Shop All | `/shop-all/` | `/merch/shop-all` | ✅ | 2026-08-03 | FIXED (#655, PR #656): dropped dark hero → white breadcrumb bar (`Home/Shop All(N)` + inline REFINE), de-duped count/REFINE. 2-col grid + real products match. Logged residuals (NOT re-filed): chip-strip-vs-REFINE user-decision; real 390 is 1-col carousel vs our shared 2-col (#629) |
| Collections index | `/collection/` | `/merch/collection` | ✅ | 2026-08-03 | FIXED (#637/#638, PR #639): stacked collection strips (MerchCollectionList — banner + rotated name tab + card row) + "All Collections" 48/600 heading. Residual (non-blocking): heading top ~20px short; 2 strips vs real 4 (fixture has 2 franchises); banners = champion-splash placeholders (real Sanity banners unavailable) |
| Collection/category | `/category/<h>` | `/merch/collection/[handle]` | ✅ | 2026-08-03 | FIXED (#657, PR #658): dropped dark hero → white breadcrumb bar (Home/<Name>(N)+REFINE), de-duped count/REFINE. Matches real /category/<h>. Logged residual (NOT re-filed): chip-strip-vs-REFINE |
| Cart | `/cart` | `/merch/cart` | ✅ | 2026-08-03 | audited DRY (real returns 500) — matches spec |
| Search | `/search` | `/merch/search` | ✅ | 2026-08-03 (re-verified) | search bar + results + empty state. Re-verified DRY (no deltas) — 1st zero-issue rotation |
| Info pages | `/faqs/`,`/shipping/`,`/returns/` | `/merch/pages/[slug]` | ✅ | 2026-08-04 | FIXED (#640/#641, PR #642): SUPPORT hero + 9-pill tab strip + typography + white bg. Residual: mascot = champion-splash placeholder. **FORM WIDGET SHIPPED (#667, PR #668):** MerchSupportForm on order-status/gift-card-balance/verify-your-product (row + lookup variants, tokened Card-of-Authenticity inline SVG, 3 new input tokens). Review NEEDS-FIXES→fixed in-lane (tokenized card hex, lookup input bg, dropped hex asset) + card render visually verified in prod. **DELTA MEASURED → #667** (fdiff-info 2026-08-04): 3 slugs are FORM widgets on real, prose stubs for us. New `MerchSupportForm` component + wiring. Measured: content wrapper max-w 1000px pad 0 32px; h2 38/700/uppercase/-0.76px; order-status=3 fields, gift-card-balance=2 fields (row layout gap 24px, inputs 256×52), verify-your-product=1 field + auth-card illustration (2-col). Buttons transparent/black/uppercase/600, 256×50, radius 0. New tokens needed for input bg #f7f7f7 / border #d0d0d0 / disabled #d1d1d1 (builder to add to merch.css). Prose slugs (faqs/shipping) confirmed correct. Mascot placeholder residual still logged |
| Sale | `/category/sales/` | `/merch/sale` | ✅ | 2026-08-03 | FIXED (#643/#644, PR #645): dropped hero → breadcrumb+grid on white; sale-price = dark current + grey #666 struck 16px + green `-NN%` badge (tokens --color-merch-badge-sale/-price-struck). Non-sale cards verified unchanged |
| Account | `/account` | `/merch/account` | ⛔ | 2026-08-03 | real is SSO wall — presentational stub, no 1:1 target |

## KNOWN RESIDUAL DELTAS (open, awaiting user call or build)
- **Filter chip-strip vs REFINE** — real shop-all/collection show only a red REFINE button; ours also renders the FilterSortBar chip strip + sort dropdown. (User-decision: drop to match, or keep.)
- **Gift-card band + franchise category strip** — stylized; no real image assets on the Sanity CDN (gift-band is a fingerprinted static; nav is text links). Not blockers.
- **Nav typography FROZEN (anti-oscillation)** — real nav is UPPERCASE (confirmed real-header.png); shipped at 16/600/uppercase (#646). Two diff agents disagreed on size/weight (16/600 vs 14/700 — likely one mis-attributed the SIGN IN button). Frozen to prevent flip-flop; #659 (revert) rejected+closed. Re-open ONLY with a single authoritative getComputedStyle read of the exact real nav link element proving a reproducible delta. DO NOT re-file from a bulk homepage diff.

## COMPONENTS
Browsable + individually inspectable at **/merch/showcase** (22 components, real merch tokens). Component-level
fidelity is validated through the PAGES that compose them; the ratchet may target a single component when a
page diff isolates the gap to one component.

---

## Rotation log (meta-loop appends one line per tick)
- 2026-08-03 — scorecard seeded from session audits (homepage pixel-audit, desktop fidelity pass, mobile pass, real-image swap, hero fix, post-real-image selfcheck = DRY). Next up: 🔁 Collections index, 🔁 Info pages.
- 2026-08-03 — ratchet: Collections index diffed → 2 deltas (#637 structural layout=carousel-strips, #638 heading). Row 🔁→⚠️. Next up: 🔁 Info pages.
- 2026-08-03 — CLOSED: #637+#638 built+shipped (PR #639, MerchCollectionList strip layout + heading). Collections index ⚠️→✅. Next up: 🔁 Info pages.
- 2026-08-03 — ratchet: Info pages diffed → real is store-chrome SUPPORT portal → 2 deltas (#640 hero+tab-nav, #641 typography+bg). Row 🔁→⚠️. Added Sale ⬜. Next up: ⬜ Sale, then re-verify.
- 2026-08-03 — CLOSED: #640+#641 built+shipped (PR #642, MerchSupportHero + MerchSupportTabStrip + white-bg fix). Info pages ⚠️→✅. Next up: ⬜ Sale.
- 2026-08-03 — ratchet: Sale diffed vs real `/category/sales/` (our `/sale/` ref 500s) → 2 deltas (#643 drop hero band, #644 sale-price dark+grey-struck+green-%-badge). Row ⬜→⚠️. Next up: re-verify oldest ✅ (round-robin) after Sale closes.
- 2026-08-03 — CLOSED: #643+#644 built (builder died post-work → controller salvaged) + reviewed (dup-count fix) + shipped (PR #645). Sale ⚠️→✅. ALL scorecard rows now ✅ (non-⛔). Next: round-robin re-verify oldest ✅ (Homepage) OR convergence-govern if 2 rotations file zero.
- 2026-08-03 — ratchet: Homepage re-verified → 2 NEW deltas (#646 nav typography, #647 stray homepage-grid REFINE/count). Row ✅→⚠️. Re-verify caught leaks the per-page audits missed → NOT converged. Next up: build #646/#647, then re-diff.
- 2026-08-03 — CLOSED: #646+#647 built (2nd builder-died-post-work → controller salvaged) + reviewed (APPROVE) + shipped (PR #648). Homepage ⚠️→✅. All rows ✅ again. Convergence needs 2 consecutive zero-issue rotations — last rotation (Homepage) filed 2, so NOT yet. Next up: round-robin re-verify next-oldest ✅ (PDP).
- 2026-08-03 — ratchet: PDP re-verified → 3 NEW deltas (#649 gallery 1:1 square/contain, #650 price 28px, #651 title uppercase) — #627/#628 under-measured. Row ✅→⚠️. Residual noted: breadcrumb bar (file next). NOT converged. Next up: build #649-#651, then re-diff.
- 2026-08-03 — CLOSED: #649+#650+#651 shipped (PR #652, gallery/price/title). Filed breadcrumb residual #653 (controller, structural). PDP stays ⚠️ #653 until breadcrumb bar moves to full-width above grid. Next up: build #653, then re-diff PDP.
- 2026-08-03 — CLOSED: #653 shipped (PR #654, breadcrumb full-width bar above grid). PDP ⚠️→✅. All rows ✅. Last diff-rotation (PDP) filed issues → still NOT converged (need 2 consecutive zero-issue rotations). Next up: round-robin re-verify next-oldest ✅ (Shop All).
- 2026-08-03 — ratchet: Shop All re-verified → 1 delta (#655 dark hero → white breadcrumb bar, same pattern as PDP/Sale). Row ✅→⚠️. NOT converged. Consistency finding: the hero→breadcrumb-bar fix landed on PDP+Sale but not Shop All. Next up: build #655, then re-diff.
- 2026-08-03 — CLOSED: #655 shipped (PR #656, Shop All white breadcrumb bar). Shop All ⚠️→✅. All rows ✅. Last diff-rotation (Shop All) filed 1 issue → still NOT converged. TECH-DEBT NOTE (non-fidelity): breadcrumb bar now hand-rolled inline in 3 pages (Sale/PDP/Shop-All) — candidate to extract a shared MerchBreadcrumbBar (own task, low-pri). Next up: round-robin re-verify next-oldest ✅ (Collection/category — may have same dark-hero delta, or legitimately keep a banner — MEASURE).
- 2026-08-03 — ratchet: Collection/category re-verified → MEASURED (not assumed): real has NO hero, white breadcrumb bar → 1 delta #657 (drop dark hero, same fix as shop-all #655). Row ✅*→⚠️. NOT converged. 4th listing page needing the breadcrumb bar (shop-all/sale/PDP/collection) — reinforces MerchBreadcrumbBar extraction. Next up: build #657, then re-diff.
- 2026-08-03 — CLOSED: #657 shipped (PR #658, Collection breadcrumb bar). Collection/category ⚠️→✅. All rows ✅. Breadcrumb bar now consistent across ALL 4 listing pages (shop-all/sale/PDP/collection) — 4 inline copies → MerchBreadcrumbBar extraction now clearly worthwhile (logged, non-fidelity). Last diff-rotation (Collection) filed 1 issue → still NOT converged. Next up: round-robin re-verify next-oldest ✅ (Cart or Search).
- 2026-08-03 — ratchet: Search re-verified → DRY (0 deltas). ✅ FIRST zero-issue rotation. Convergence needs ONE MORE consecutive zero-issue rotation. (Cart real page 500s — non-diffable like Account ⛔.) Next up: re-verify another oldest ✅ (Homepage/PDP/etc.) — if DRY → CONVERGED.
- 2026-08-03 — ratchet: Homepage re-verify #2 → 2 real deltas (#660 announcement pill+marquee never built; #661 logo lockup order reversed) + 1 REJECTED (#659 nav-typo revert = oscillation vs #646; supervisor closed it, nav frozen — see KNOWN RESIDUAL). Row ✅→⚠️. Convergence counter RESET (this rotation filed issues). Sale-badge/#647 verified no-regression. Next up: build #660/#661, then re-diff.
- 2026-08-03 — CLOSED: #660+#661 shipped (PR #662, announcement pill+marquee + logo order). Homepage ⚠️→✅. All rows ✅. Convergence counter: Search DRY (rotation N-1), Homepage#2 filed issues (rotation N) → RESET; need 2 consecutive DRY again. Noted for follow-up: announcement dismiss ✕ may not fire (pre-existing). Next up: re-verify oldest ✅ (PDP/Shop-All etc.).
- 2026-08-04 — merch-gap completeness pass (page-TYPE level): DRY on missing pages — all 9 real page types cloned. BUT surfaced a fidelity delta: Info pages ✅→⚠️ — 3 support slugs (order-lookup / gift-card-balance / product-validation) are FORM-first on real, prose stubs for us. Not a new page (correctly not filed as one); fidelity-engine to measure+file. Next fidelity tick should prioritize Info pages re-diff to capture the form widget.
- 2026-08-04 — ratchet: Info pages diffed (fdiff-info) → DELTA MEASURED, filed #667 (MerchSupportForm component + wiring, full field specs @ 1280+390, 3 new input tokens). Row stays ⚠️ #667 until built. Prose slugs confirmed no-regression. This rotation filed 1 issue → NOT converged. Next up: build-loop builds #667; then re-diff Info pages to confirm, OR round-robin Homepage 🔁 re-verify. (Deploy of #663/#664/#666 still pending Vercel quota reset.)
- 2026-08-04 — CLOSED: #667 built+shipped (PR #668, MerchSupportForm). Review NEEDS-FIXES (1 Crit tokenize card SVG hex + 2 Imp: lookup input bg token, drop dead hex asset) → fixed in-lane + card render visually verified in prod → merged. Info pages ⚠️→✅. All non-⛔ rows ✅ EXCEPT Homepage 🔁 (hero rework awaiting re-verify). Last rotation filed an issue → NOT converged. Deploy queue now 4 (#663/#664/#666/#668) pending Vercel quota. Next up: Homepage 🔁 re-diff (verify #664 hero unification pixel-matches real @ 1280+390).
- 2026-08-03 — USER-DRIVEN (not a diff-tick): homepage hero/strip STRUCTURAL rework shipped (PR #664) — franchise strip is the real hero's slide-control bar, not a separate band. 8 real logo SVGs extracted (Playwright, zero fallbacks) + clip-path square-left first tile + 40px content gutters (bg full-bleed) + tiles select hero slides. Review APPROVE (in-lane fix: dropped hardcoded SVG ids, added ArcaneLogo viewBox). Homepage ✅→🔁 (surface changed → re-diff queued). Convergence counter RESET. DEPLOY PENDING (Vercel 100/day quota exhausted — batch #663+#664 when it resets ~24h). Also logged (non-fidelity audit): 5 pre-existing merch *.showcase.tsx carry 'use client' (server-safe rule violation — info-page/size-guide/sign-in/support-tab/support-hero). Next up: after deploy lands, re-diff Homepage vs real (1280+390); then round-robin oldest ✅.
- 2026-08-04 — ratchet: Homepage re-verify #3 (post #664 hero/strip unification) — holistic Playwright pixel-diff at 1280+390. CONFIRMED MATCH on: clip-path shape (polygon identical on inner divs), tile h=64px, tile w=170px, first-tile left gutter=40px desktop, logo sizes (75×29/80×25/etc.), active tile underline bar, progress bar colors, hero bg full-bleed/cover, hero height ~535px (+5px ours = tolerance). 2 NEW DELTAS: #669 (bar flush hero bottom — real has 40px art below; nav-tabs bottom=625, hero bottom=665); #670 (mobile hero 520px vs real 544px, mobile gutter 40px vs 24px). Homepage 🔁→⚠️. NOT converged (rotation filed issues). No nav-typo re-file (FROZEN). Next up: build #669+#670, then re-diff.
- 2026-08-04 — CLOSED: #669+#670 built+shipped bundled (PR #671, one builder — both touch merch-hero-banner.tsx). Bar bottom-0→bottom-10 (40px art below), mobile aspect-[195/272] (544px) + pl-6 md:pl-10 (24px mobile gutter). Diff clean (1 file, no hex, gates green), both fixes visually verified at 1280+390. Homepage ⚠️→🔁 (re-diff to confirm the fixes match → if DRY, first clean rotation toward convergence). Deploy queue now 5 (#663/#664/#666/#668/#671) pending Vercel quota. Next up: re-diff Homepage; if DRY, re-verify oldest ✅ (PDP) for the 2nd consecutive DRY needed to CONVERGE.
- 2026-08-04 — ratchet: Homepage CONFIRM re-diff (fdiff-home2, post #671) → DRY / MATCH. #669 confirmed (hero 540 vs real 535, 5px tol), #670 confirmed (mobile hero 544 EXACT, first-tile gutter 24px EXACT, no 390 overflow), holistic sweep no new deltas. Homepage 🔁→✅. ALL non-⛔ rows now ✅. This is the 1st consecutive ZERO-issue diff-rotation — need ONE MORE DRY diff-rotation to CONVERGE. Next up: re-verify OLDEST ✅ (PDP, 2026-08-03) — if DRY → 2nd consecutive → CONVERGED.
- 2026-08-04 — ratchet: PDP re-verify (fdiff-pdp) → title/price/gallery/2-col all MATCH, BUT caught breadcrumb-bar under-measurement (agent reported DRY; supervisor OVERRODE — the deltas are beyond tolerance: 14px grey vs real 16px black text, 24 vs 40 padding, 60 vs 40 mobile height — a code-comment documenting a value ≠ matches real). Filed #672: extract MerchBreadcrumbBar w/ correct values + apply to all 4 pages (shop-all/sale/PDP/collection) — also closes the long-logged 4-inline-copy tech-debt. PDP ✅→⚠️ #672. Convergence counter RESET (rotation filed 1 issue) — re-verify did its job (caught a real under-measure, same class as the earlier PDP #649-#651 catch). Next up: build #672, then re-diff PDP + a listing page to confirm the shared bar.
- 2026-08-04 — CLOSED: #672 built+shipped (PR #673, MerchBreadcrumbBar extraction + fidelity fix). Review APPROVE (per-page wiring correct on all 4 pages, measured 16px black/40px pad/60-40 height exact, no 390 overflow, gates green). Deployed (quota reset) + 4 breadcrumb routes 200. PDP ⚠️→🔁 (re-diff to confirm the shared bar). Also resolved the long-logged 4-inline-copy breadcrumb tech-debt. Minor non-blocking: dead wrapper div w/ undefined --merch-bar-h var (trivial cleanup, no effect). Convergence counter: last diff-rotation (PDP) filed #672 → RESET; need 2 fresh consecutive DRY diff-rotations. Next up: re-diff PDP (or a listing page) to confirm the breadcrumb bar → 1st clean.
- 2026-08-04 — ratchet: PDP CONFIRM re-diff (fdiff-pdp2, post #672/#673) → DRY / MATCH. Breadcrumb 16px/rgb(26,26,26)/40px/60-40 EXACT at 1280+390; gallery/title/price all MATCH; no 390 overflow. PDP 🔁→✅. ALL non-⛔ rows now ✅ again. CONVERGENCE NOTE: agent claimed CONVERGED but that's premature — the diff-rotation sequence is Homepage-DRY → PDP-reverify-FILED-#672 (reset) → PDP-confirm-DRY, so this is the 1st clean diff-rotation AFTER the #672 reset, not the 2nd. Also the shared MerchBreadcrumbBar (#673) landed on 4 pages but only PDP was re-diffed post-change — a LISTING page should confirm the shared bar in its own context. NOT converged. Next up: re-diff a listing page (Shop All, oldest ✅ 2026-08-03) to confirm the shared breadcrumb + listing layout → if DRY, 2nd consecutive clean diff-rotation → CONVERGED.
