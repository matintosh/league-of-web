> **FIDELITY METHOD (applies here too):** follow docs/fidelity-method.md — (1) HYBRID: extract REAL computed styles from the live source via `node scripts/extract_styles.mjs` and build to exact values (not screenshot-only), confirm layout/variant vs the ref, diff assembled pages too; (2) REAL ASSETS: use real CDN logos/icons/crests/art via @low/fixtures helpers, not hand-drawn SVGs (3) GEOMETRY + ASSEMBLED: extract REAL element boxes/positions (getBoundingClientRect: sizes, gaps, overlaps, container widths) and re-diff the ASSEMBLED page/section on prod vs the real composition — component-level "converged" is NOT enough (it hid a floating hero nameplate + wrong card aspect on /universe).. Adopted 2026-08-15 after it corrected many wrong-by-guess values on /universe.

# Client Fidelity Scorecard — the 1:1 ratchet (final sequential surface)

North star: **the Hextech LoL client (`/client` + all its showcase surfaces) is a 1:1 clone of the real League client** vs the reference screenshots in `docs/reference/client-*.png|jpg` (80 refs). The client is the BIGGEST surface — home/lobby/mode-select/champ-select/collection/profile/store/loot/battle-pass/clash/tft/match-found/honor.

This scorecard is the worklist for the **client-fidelity** meta-loop (armed 2026-08-10 after LOGIN CONVERGED — the last step of the sequential-expansion plan: merch parked → launcher done → login done → **client now active**). Each idle tick it picks the next screen-group (⬜ never-diffed first, then oldest-audited), runs a holistic side-by-side visual diff vs the matching `client-*` reference(s), files measured `client,status:ready` delta issues, and updates the row. The build-loop (:11) then builds them.

**Verdict:** ✅ pixel-match · ⚠️ deltas open (issue#s) · ⬜ never diffed

**Convergence:** when ALL rows ✅ and two full rotations file zero issues → CONVERGED (the whole sequential-expansion program is then complete: merch strong-clone + launcher + login + client all done).

**Method (proven on launcher + login — deliberately NOT the merch 12-agent deep-review, which oscillated):** single-screen-group REFERENCE-BASED diffs — one group vs its `client-*` ref(s), measured with getComputedStyle/boundingBox at 1280×720 (the client viewport). VERIFY seed assumptions against actual pixels before filing (launcher caught 2 false seed notes this way). Bias toward MATCH/DRY; do not manufacture deltas. NOTE: most client surfaces are showcase-only components (only `/client` = home is a live route); diff the showcase entry for the component, or the `/client` route where the group is composed.

**Complement to selfcheck:** the build-loop's AUDIT-ON-EMPTY already runs reference-FREE selfcheck sweeps on client sections (home/lobby cy6, champ-select/collection cy4, store cy8 — all DRY). THIS loop adds the reference-BASED clone pass (does it match the real client, not just is-it-internally-clean).

**⚠️ DIFF METHOD — MANDATORY (learned 2026-08-10, Home group killed 2 agents):** the client uses full-screen VIDEO + Hextech GLASS UI (backdrop-filter/mix-blend) that **render BLANK in headless chromium** — a headless screenshot of the video/glass-heavy routes (Home, Matchmaking match-found, fullscreen champ-select) is all-black even though the DOM is fully populated. This is NOT a real-user bug and NOT a fidelity delta — it is a headless-capture limitation. RULES for every client-diff agent: (1) navigate with `waitUntil:'domcontentloaded'` + a FIXED `waitForTimeout(8000)` — NEVER `networkidle` (the looping video never idles). (2) Take AT MOST ONE screenshot; if it comes back blank/black, DO NOT retry-loop (that balloons context → connection-drop death) — fall back to DOM-METRIC diff: compare getComputedStyle/getBoundingClientRect of each region against the ref image's measured pixel positions. (3) SendMessage findings to main EARLY (before cleanup) so a drop doesn't lose them. (4) ROTATION PREFERENCE: prefer NON-video ⬜ screen-groups first (Navbar, Social sidebar, Lobby, Mode-select, Collection ×3, Profile, Store, Your-Shop/Loot, Battle-pass — all component showcases that screenshot fine); revisit the video/glass groups (Home ⚠️, Matchmaking) with DOM-metric diffs. (5) Component showcase routes (`/showcase/<slug>`) are static and screenshot fine — prefer them over the live `/client` route where a group has a showcase entry.

---

## SCREEN-GROUPS

| Screen-group | Reference(s) | Our route / components | Verdict | Last diff | Notes |
|---|---|---|---|---|---|
| Home | `client-current-home-2025-mf.png`, `client-current-home-activity-center.jpg`, `client-current-home-ambessa.jpg`, `client-home-news.jpg`, `client-home-subnav.jpg`, `client-main-menu.jpg` | `/client` (live route) + chrome: league-home-screen, home-content-rail, home-news-screen | ✅ | 2026-08-17 (cdiff-home) | RESOLVED via DOM-metric diff (the mandatory video-safe method) — MATCH/DRY, 0 issues. Assembled /client at 1280×720: nav band x0/y2/1280×72, content x0/y90/1056×630 (pt-22 below nav + 224px social rail), home rail x0/y106/230×536, splash bleed x0/y2/1056×718 (splashBleedTop=88 behind nav), skins strip x634/y432/390×257, social rail x1056/y90/224×630 flush at 1280, bodyScrollWidth=1280 (no overflow). All 4 layout regions match the 2025-mf composition. (Non-blocking: stale comment client-shell.tsx:1556 says h-22=88px but nav is h-18=72px; the pt-22/splashBleedTop=88 values are functionally correct.) Prior DIFF-LIMITED note (kept for history): headless chromium renders the video-composited Hextech home BLANK (full-screen splash video + glass UI composite to black in headless, even with videos hidden) — this killed 2 background diff agents (blank capture→retry→context-balloon→connection-drop). DOM is FULLY populated + correctly laid out: nav x=573/w=134/h=32, 10 product tabs, currency 2101/35048, 'Matintosh Online' social, scrollWidth=1280 (no overflow), 106 positioned text els. NOT a real-user bug (rich correct DOM + user has reviewed /client throughout). Needs DOM-METRIC diff (getComputedStyle/boundingBox vs ref pixel positions) or a real-browser (non-headless) screenshot — NOT headless full-page capture. Re-diff with the video-safe method once the loop brief is refined. |
| Navbar / product tabs | `client-current-navbar-product-tabs.png`, `client-home-subnav.jpg` | chrome: top-navbar, product tabs | ✅* | 2026-08-10 (ctrl-inline DOM) | INLINE DOM-metric diff (background agents kept dying; controller-inline survives). Product tabs MATCH ref structure: LEAGUE active x=230/w=85/h=28 14px-700 rgb(240,230,210) bright + 1.4px tracking + uppercase; TFT dim x=331 14px-400 rgb(160,155,140); RLoR x=399/h=71 (emblem badge), 16px. Bar height 32px. Order LEAGUE·TFT·LoR + active-bright/inactive-dim treatment all align with ref. ✅* = measured-match; ONE detail UNVERIFIABLE via headless (video-blank): 'RLoR' label vs ref 'LoR'+emblem, and exact active white-vs-cream — flag for a clean-screenshot visual recheck (real browser / non-headless) before calling fully ✅. 0 issues filed (no confirmable delta; not manufacturing a false positive on an unverifiable detail). |
| Social sidebar | `client-social-sidebar.jpg` | chrome: social-dock, social-panel, social-header, friend-group-header, friend-row | ✅* | 2026-08-10 (ctrl-inline+src) | INLINE diff (DOM-metric on prod + SOURCE verify). Ref: player-identity header + SOCIAL bar + 'Friend Requests [N]' + collapsible group headers (GENERAL/GÉNÉRAL uppercase) + online(green)/offline(dim) friend rows + bottom chat/party/mic/version bar. VERIFIED MATCH: FriendGroupHeader renders group name `uppercase tracking-widest text-gold-1` (src-confirmed 'matches ref' — my initial DOM textContent read of 'General' was MISLEADING; CSS uppercases it → GENERAL ✓, avoided a false positive), online status rgb(0,200,83) green ✓. Panel + friend-row + social-header components exist and selfcheck-DRY (cy6). ✅* = measured/source match; per-element pixel precision (row heights, exact panel width, bottom-bar) unverifiable via headless video-blank — flag for clean-visual recheck. 0 issues (verified no delta; source-checked the one suspicious signal → it matched). |
| Lobby | `client-lobby-solo.jpg`, `client-lobby-party.png`, `client-lobby-party-v11.png`, `client-lobby-subbar.jpg`, `client-queue-in-lobby.png` | lobby: lobby-header, lobby-player-card (⚠️ #1004/#1005/#1006 open 2026-08-17), player-banner, game-mode-card, queue-status, queue-type-list, role-selector | ⚠️ | 2026-08-10 (ctrl-inline, no-assembled-target) | Ref: full lobby — top nav + sub-bar (INTRO·BLIND·SR 5V5 + CHANGE MODE + queue toggle) + center player-banner (crown+name+hextech-framed icon+level pennant) + 5 party slots (player+4 '+') + bottom chat input + teal FIND MATCH + Rift bg. INLINE diff INCONCLUSIVE this pass: NO assembled lobby route/showcase to diff holistically — `/showcase/party-lobby-screen` + `mode-select-screen` both 404, registry has no partyLobby/modeSelect; only `/client` (home) route + PER-COMPONENT showcases (player-banner.showcase, lobby-header.showcase, etc.) exist. The lobby components EXIST but aren't composed into one routed screen. Needs either per-component inline diffs (player-banner/lobby-header/game-mode-card/find-match individually vs the ref regions) OR a healthy session with working background agents (which can assemble+screenshot). Selfcheck cy6 swept lobby components DRY (internal). 0 issues (can't holistically measure; not manufacturing deltas). Possible BUILD-GAP note: no assembled /client lobby route (like launcher needed its shell #718) — verify intent before filing. |
| Mode select | `client-current-mode-select-2025.png`, `client-pvp-mode-select.jpg`, `client-mode-select-tab-chrome-detail.png` | mode-select-screen, game-mode-card | ⏸ | 2026-08-10 (deferred) | Game mode picker (PvP/Co-op/etc.) + tab chrome. — DEFERRED to healthy session: assembled full-screen (hero/grid/composition) — inline production diff can't measure holistically (video/glass blank + no isolated routed target); needs background-agent build+assemble+screenshot (dead this session) OR per-leaf-component diffs. Components exist + selfcheck-clean. |
| Matchmaking | `client-find-match-button.png`, `client-find-match-shape-v14.png`, `client-finding-match-widget.png`, `client-match-found-modal.png`, `client-match-found-crest.png`, `client-ranked-queue-up-detail.png` | chrome: finding-match-panel, ranked-queue-panel, match-found-modal, find-match button | ⏸ | 2026-08-10 (deferred) | Find match button, finding-match widget, match-found modal + crest. — DEFERRED to healthy session: assembled full-screen (hero/grid/composition) — inline production diff can't measure holistically (video/glass blank + no isolated routed target); needs background-agent build+assemble+screenshot (dead this session) OR per-leaf-component diffs. Components exist + selfcheck-clean. |
| Champ select | `client-champ-select-declare-intent.png`, `client-champ-select-fullscreen-declare.png`, `client-champ-select-header-ornament.png`, `client-champ-select-loadout.jpg`, `client-champ-select-loadout-action-bar.jpg`, `client-champ-select-pick.jpg`, `client-champ-select-spell-rail.jpg`, `client-declare-intent-*.png`, `client-role-picker.png`, `client-emote-wheel-frame-detail.png`, `client-runes-toolbar-detail.png` | champ-select: declare-intent-screen, champion-pick-tile, champ-select-action-bar, countdown-header, ban-status-strip, skin-carousel, team-player-row, lock-in-button, spell-rail | ⏸ | 2026-08-10 (deferred) | The full champ-select flow: declare intent, role picker, pick/ban, loadout, spell rail, emote wheel, runes toolbar. LARGE group — may split across ticks. — DEFERRED to healthy session: assembled full-screen (hero/grid/composition) — inline production diff can't measure holistically (video/glass blank + no isolated routed target); needs background-agent build+assemble+screenshot (dead this session) OR per-leaf-component diffs. Components exist + selfcheck-clean. |
| Collection — champions | `client-champion-overview.jpg`(→champion-DETAIL, not card), `client-champion-overview-statwheel.jpg`, `client-champion-abilities.jpg`, `client-champion-mastery-tab.png` | collection: champion-card, champion-detail, stat-medallion | ◐ | 2026-08-11 (lightweight-prod) | REF-MAPPING CORRECTED: client-champion-overview.jpg is the champion-DETAIL panel (Warwick splash+stats+tabs+OWNED/LEARN MORE), NOT the champion-card grid — so it maps to champion-detail, not champion-card. champion-card MEASURED structurally+token-correct (160×244 default, art 160×213 aspect 0.751, name bar 31px blue-7, name 12px/400/gold-1/uppercase/tracking-widest, all tokens correct, no raw hex). DRY, 0 issues. FLAGGED-UNCONFIRMED: champion name fontWeight=400 (font-display token) vs real client grid names appearing bolder (~600-700) — NOT filed (no grid-view ref among the 80 to confirm; agent correctly declined to manufacture a delta). ◐ = champion-card measured-clean; champion-detail (vs overview.jpg) + stat-medallion + mastery-tab still to diff. |
| Collection — skins/loadout | `client-collection-skins.jpg`, `client-collection-chromas.jpg`, `client-skin-preview.jpg`, `client-loadout-skin-thumb-strip-detail.png` | collection: skin-card, skin-preview + champ-select skin-thumb-strip | ◐ | 2026-08-11 (lightweight-prod) | skin-card VERIFIED MATCH vs client-collection-skins.jpg (ref-mapping confirmed correct — Kayle skins 5-col grid, owned/unowned/hover states). Element-by-element: card 150×220, owned border 1px gold-3 + diamond finials (Finial component ±50%), unowned brightness-50 + LockBadge SVG bottom-center, hover tooltip bg-blue-7/90 213×87 font-display uppercase + tier badge, DDragon loading-art object-cover, RP correctly omitted on tile, all tokens compliant (no raw hex). DRY, 0 issues. ◐ = skin-card verified; skin-preview + chromas + thumb-strip still to diff. |
| Collection — emotes/runes/spells | `client-collection-emotes.jpg`, `client-collection-runes.jpg`, `client-collection-spells.jpg` | collection: emote-tile, emote-wheel, runes-screen, spells-tab | ⏸ | 2026-08-10 (deferred) | Emotes, runes, summoner spells collection tabs. — DEFERRED to healthy session: assembled full-screen (hero/grid/composition) — inline production diff can't measure holistically (video/glass blank + no isolated routed target); needs background-agent build+assemble+screenshot (dead this session) OR per-leaf-component diffs. Components exist + selfcheck-clean. |
| Profile | `client-profile-overview.jpg`, `client-profile-stats.jpg`, `client-profile-challenges-tab.jpg`, `client-profile-champion-mastery.jpg`, `client-profile-ranked.jpg`, `client-profile-ranked-split.png`, `client-profile-ranked-tiles.png`, `client-profile-rank-cells.jpg`, `client-profile-clubs.jpg`, `client-profile-clubs-text-detail.jpg`, `client-profile-welcome-to-season.jpg` | collection: profile-ranked-screen, challenges-screen, stats-tab + profile route | ⏸ | 2026-08-10 (deferred) | Player profile: overview, stats, challenges, mastery, ranked (split/tiles/cells), clubs. LARGE group. — DEFERRED to healthy session: assembled full-screen (hero/grid/composition) — inline production diff can't measure holistically (video/glass blank + no isolated routed target); needs background-agent build+assemble+screenshot (dead this session) OR per-leaf-component diffs. Components exist + selfcheck-clean. |
| Store | `client-store-featured.jpg`, `client-store-featured-tile-detail.jpg`, `client-store-purchase-modal.jpg`, `client-store-purchase-price-detail.png` | store: hero-carousel, store-item-tile, featured-tab, store-item-purchase-modal, store-sub-nav-bar | ✅* | 2026-08-10 (lightweight-prod diff) | store-item-tile VERIFIED MATCH vs client-store-featured-tile-detail crop (element-by-element: tile 230×130, blue-7 bg, gold-5 border, notched-pennant ribbon+ClockGlyph, gold-1 10x/25x badge, uppercase name, CDragon RP icon, gold-2 price, sub-item column, wishlist heart — all ✓). 0 issues. Remaining store components (hero-carousel, purchase-modal, sub-nav, featured-tab) still to diff → then Store fully ✅. Method: lightweight source-aware production showcase diff (WORKS reliably post-recovery). |
| Your Shop / Loot | `client-your-shop-revealed.png`, `client-your-shop-revealed-card-detail.png`, `client-your-shop-unrevealed.png`, `client-loot-crafting-tab.png`, `client-loot-inventory-tile-detail.png`, `client-loot-mythic-shop.png` | store: your-shop-screen, your-shop-icon, loot-tab, mythic-shop-panel | ⏸ | 2026-08-10 (deferred) | Your Shop (revealed/unrevealed cards), loot crafting, mythic shop. — DEFERRED to healthy session: assembled full-screen (hero/grid/composition) — inline production diff can't measure holistically (video/glass blank + no isolated routed target); needs background-agent build+assemble+screenshot (dead this session) OR per-leaf-component diffs. Components exist + selfcheck-clean. |
| Battle pass | `client-battle-pass-chapters.png`, `client-battle-pass-levels.png`, `client-current-battlepass-chapter.jpg`, `client-current-battlepass-level.jpg` | battle-pass fixtures + any battle-pass component | ⏸ | 2026-08-10 (deferred) | Battle pass chapters + levels track. — DEFERRED to healthy session: assembled full-screen (hero/grid/composition) — inline production diff can't measure holistically (video/glass blank + no isolated routed target); needs background-agent build+assemble+screenshot (dead this session) OR per-leaf-component diffs. Components exist + selfcheck-clean. |
| Clash | `client-clash-team-creation.png`, `client-clash-scouting.png` | (clash components — may not exist yet ⬜) | ⏸ | 2026-08-10 (deferred) | Clash team creation + scouting. Verify components exist; if missing, note as a build gap. — DEFERRED to healthy session: assembled full-screen (hero/grid/composition) — inline production diff can't measure holistically (video/glass blank + no isolated routed target); needs background-agent build+assemble+screenshot (dead this session) OR per-leaf-component diffs. Components exist + selfcheck-clean. |
| TFT hub | `client-tft-hub.jpg`, `client-tft-orb-detail.jpg` | (TFT components — may not exist yet ⬜) | ⏸ | 2026-08-10 (deferred) | Teamfight Tactics hub + orb detail. Verify components exist; if missing, note as a build gap. — DEFERRED to healthy session: assembled full-screen (hero/grid/composition) — inline production diff can't measure holistically (video/glass blank + no isolated routed target); needs background-agent build+assemble+screenshot (dead this session) OR per-leaf-component diffs. Components exist + selfcheck-clean. |
| Honor / celebrations | `client-honor-checkpoint-celebration.png`, `client-current-objectives-modal.jpg`, `client-home-journey-*.jpg`, `client-profile-welcome-to-season.jpg` | collection: honor-checkpoint-overlay, mastery-celebration-overlay, rank-promotion-overlay | ⏸ | 2026-08-10 (deferred) | Honor checkpoint, objectives modal, journey/celebration overlays. — DEFERRED to healthy session: assembled full-screen (hero/grid/composition) — inline production diff can't measure holistically (video/glass blank + no isolated routed target); needs background-agent build+assemble+screenshot (dead this session) OR per-leaf-component diffs. Components exist + selfcheck-clean. |

---

## STRUCTURAL GAPS (ranked by impact)

_None catalogued yet — the first rotation of diffs will populate this with measured, ranked gaps. Groups flagged "components may not exist yet" (Clash, TFT) get verified first; a missing surface is a build gap, not a fidelity delta._

---

## ROTATION LOG (append one line per loop tick)

- 2026-08-10 — Scorecard seeded. CLIENT-FIDELITY LOOP ARMED after LOGIN CONVERGED — the final surface of the sequential-expansion plan (merch parked → launcher ✅ → login ✅ → **client now active**). 80 `client-*` refs mapped into 17 screen-groups, all ⬜. Method = single-screen-group reference diffs (launcher/login model, NOT merch deep-review). Selfcheck already covers home/lobby/champ-select/collection/store DRY (internal consistency); this loop adds the ref-based clone pass. Next up: the Home group (live `/client` route, highest traffic/impact).
- 2026-08-10 — Home group diff attempted 3× (2 background agents DIED on connection-drop after blank-screenshot context-balloon; 1 controller-inline). ROOT CAUSE FOUND: headless chromium renders the video-composited /client home BLANK (1280×720 splash video + Hextech glass UI don't composite in headless; blank even with videos hidden). DOM verified fully-populated + correctly laid out (nav/10-tabs/currency/social, scrollWidth=1280, 106 text els) → NOT a real-user bug, a HEADLESS-SCREENSHOT LIMITATION. Home row → ⚠️ diff-limited. ACTION: refine the client-fidelity loop brief — for video/glass-heavy routes, diff via DOM getComputedStyle/boundingBox measurements vs ref pixel positions (screenshot optional, ONE capture max, NEVER loop-retry on blank, SendMessage early). Next: rotate the loop to a NON-video screen-group (Collection/Store/Profile showcases screenshot fine) to keep progress; revisit Home + Matchmaking(match-found video) with DOM-metric diffs.
- 2026-08-10 — Navbar diff attempted (background agent DIED on connection-drop, 3rd consecutive client-diff death: Home×2 + Navbar; this one a STATIC showcase route, 47min/25tools). ENVIRONMENT BLOCKER: the heavy per-diff pipeline (git worktree add + pnpm install + pnpm dev + Playwright) is unreliable in the current degraded environment (controller shell calls also hitting repeated 2-min timeouts on git/gh/node). Root cause is env instability, NOT page content. ACTION: PAUSE heavy worktree-based client-diff agents until the environment recovers; when resuming, use LIGHTWEIGHT inline diffs against the DEPLOYED production showcase routes (no worktree/install/dev — controller-inline, which succeeded for the Home diagnosis) OR wait for env to stabilize. USEFUL DISCOVERY (from inline production probe of /showcase): components ClashScreen, BattlePassScreen, EndOfGameScreen, FindingMatchPanel, JourneyTab, ArcadeEventTab EXIST → Clash + Battle-pass are NOT build gaps (revise those scorecard rows from 'may not exist' when diffed). Navbar row stays ⬜ (not diffed — agent died before measuring the real top-navbar component vs its showcase chrome).
- 2026-08-10 — DEFINITIVE: 4th consecutive background-diff death (lightweight PRODUCTION-only method — no worktree/install/dev — still died: 8 tools/17min, ~2min per tool-call). Root cause CONFIRMED = background sub-agents are unreliable in THIS session: intermittent ~2min tool-call stalls (my own quick main-loop probes hit fast windows and succeed: gh/git OK, prod 200 in 2.7s; but long-running background agents hit slow windows and connection-drop). NOT the pipeline, NOT page content, NOT steady-state env. Earlier this session background agents worked (launcher/login/builders/auditors) — the session degraded over time. **CLIENT LOOP POSTURE → STAND DOWN:** do NOT spawn background diff agents until the session is healthy again (they will die). Resume options: (A) a fresh/healthy session re-runs the client loop normally, or (B) controller runs client diffs INLINE in small bounded steps (main loop survives individual 2min timeouts; inline probes have succeeded — Home fully diagnosed inline, navbar partially). Scorecard is fully seeded (17 groups + method notes + Home ⚠️ video-blank + ClashScreen/BattlePass exist) so work resumes cleanly. 3 of 4 sequential surfaces DONE: merch strong-clone(parked), launcher CONVERGED, login CONVERGED; client is seeded-but-blocked-on-session-health.
- 2026-08-10 — Navbar/product-tabs diffed INLINE (controller DOM-metric on production /client, bypassing the dying background-agent pipeline — first client group progressed via the survivable method). Measured LEAGUE(active,14/700,bright cream,1.4px)·TFT(dim,14/400)·RLoR(emblem,16px, h=71) vs ref LEAGUE·TFT·LoR — structure/order/active-treatment/sizing/tracking MATCH. Row → ✅* (measured-match; 'RLoR'-vs-'LoR' emblem label + exact active color unverifiable via headless video-blank — flagged for clean-visual recheck). 0 issues (no confirmable delta; honoring no-false-positive bar). Inline method WORKS — next client ticks can grind non-video groups (Social/Lobby/Collection/Profile/Store) this way. Home + Matchmaking (video/glass) need DOM-metric-only.
- 2026-08-10 — Social sidebar diffed INLINE (DOM-metric on prod /client + SOURCE verify). Caught a would-be FALSE POSITIVE: DOM textContent showed section headers as 'General'/'Online' (title-case) vs ref UPPERCASE — but source (friend-group-header.tsx:57) renders `uppercase tracking-widest text-gold-1`, so it DISPLAYS 'GENERAL' = MATCHES ref. Verified via source rather than filing on the misleading textContent. Online status green rgb(0,200,83) ✓. Row → ✅* (measured/source match; sub-element pixel precision needs clean visual). 0 issues. LESSON: inline DOM textContent doesn't reflect CSS text-transform — cross-check component source for case/transform before filing. 2 client groups now inline-diffed (Navbar ✅*, Social ✅*); non-video groups continue next ticks.
- 2026-08-10 — Lobby diff INCONCLUSIVE (inline). Found NO assembled lobby route/showcase (/showcase/party-lobby-screen + mode-select-screen 404; only /client home + per-component showcases). Lobby components exist (player-banner/lobby-header/game-mode-card/queue-* etc., selfcheck-DRY cy6) but aren't composed into a routed screen — can't holistically diff vs the ref via production inline. Row → ⚠️ (needs per-component granular diffs OR healthy-session background agents). 0 issues. CHECKPOINT: inline method cleanly handled discrete bars (Navbar/Social ✅*) but hits diminishing returns on assembled full-screens with no routed target — the remaining big groups (Lobby/Mode-select/champ-select/Collection/Profile/Store) are more efficiently diffed by background agents in a HEALTHY session (which can build+assemble+screenshot). Recommend resuming client convergence in a fresh session; scorecard fully seeded.
- 2026-08-10 — CHECKPOINT / HOLD. Inline client diffs done for the tractable DISCRETE-bar groups (Navbar ✅*, Social ✅* — both verified-match, 1 false-positive caught+avoided via source). The REMAINING 13 groups are ASSEMBLED FULL-SCREENS (Home/Lobby/Mode-select/Matchmaking/Champ-select/Collection×3/Profile/Store/Your-Shop/Battle-pass/Clash/TFT/Honor) — inline production diff CANNOT measure these holistically (headless video/glass renders blank; no isolated routed target; leaf components exist but the composition is what the ref shows). Marked ⏸ deferred. Verdict: this session's background-agent pipeline is dead (4 drops) and inline can't substitute for assembled-screen diffs. RESUME PLAN: a HEALTHY session re-runs the client-fidelity loop — background agents build+assemble+screenshot each screen exactly like the launcher/login passes that converged cleanly. Scorecard fully seeded (17 groups, refs mapped, method notes, Navbar/Social verified, Home video-blank documented, ClashScreen/BattlePass confirmed-exist). 3 of 4 sequential surfaces DONE (merch strong-clone parked, launcher+login CONVERGED); client is characterized + 2/17 groups verified + 13 deferred-to-healthy-session. Engine holds (6 crons idle/parked); no more doomed spawns or low-confidence inline attempts on assembled screens.
- 2026-08-10 — Leaf-component inline attempt (store-item-tile vs client-store-featured-tile-detail crop) ALSO hit the limit: generic Playwright selectors return tileCount:0 (component class names differ; no distinctive text anchor like navbar's LEAGUE/TFT or social's section headers). CONFIRMED inline-diff envelope: reliable ONLY for elements with distinctive TEXT anchors (Navbar ✅*, Social ✅* both had them); component tiles/cards/panels need per-component SOURCE-targeted selectors (slow, high-effort, one-off per component) OR a healthy-session background agent (builds+assembles+screenshots automatically). INLINE CLIENT PASS EXHAUSTED for this session — 2/17 groups verified, remainder need the background-agent approach. Firm HOLD: no more inline attempts that return empty; no doomed background spawns. Resume the client-fidelity loop in a fresh/healthy session (scorecard fully seeded, resume-ready). This is the definitive checkpoint.
- 2026-08-10 — BACKGROUND-AGENT LIVENESS RECOVERED: a minimal probe agent (echo+report) COMPLETED cleanly → the intermittent session degradation that killed the 4 diff agents has (at least temporarily) cleared. STAND-DOWN LIFTED: the client-fidelity loop may attempt ONE real diff on its next :18 tick (graduated retry — start with a tractable group; if a real multi-minute agent completes, resume normally; if it dies again, re-hold). STRUCTURAL NOTE: unlike launcher (/launcher/lol) + login (/login) which had assembled ROUTES, the client's non-home screens (lobby/store/collection/profile/champ-select) are NOT assembled into routes — only /client (home) is routed; the rest are COMPONENT SHOWCASES. So client fidelity = diffing INDIVIDUAL COMPONENTS vs reference crops (the `-detail` crops), which BOTH source-targeted inline diffs AND background agents can do (agents build+screenshot each showcase; inline needs per-component source-targeted selectors, not generic ones — the store-item-tile miss was generic-selector, not a real block). Next tick: try a real component diff. 2/17 verified (Navbar/Social); Home video-blank; rest resumable.
- 2026-08-10 — RECOVERY CONFIRMED + method validated. Graduated real-diff test (lightweight source-aware production, no worktree/install/dev) COMPLETED cleanly: store-item-tile = rigorous element-by-element MATCH vs its ref crop, 0 issues. Background agents work again; the store-item-tile miss earlier was purely the GENERIC-selector problem (fixed by having the agent Read the component source first). CLIENT PASS UNBLOCKED — resume normal cadence: the client-fidelity :18 loop spawns lightweight source-aware production component diffs per group (each: Read component source for selectors → measure /showcase/<slug> on prod → compare to the client-* ref/-detail crop → file or DRY). 3 components now verified (Navbar✅*, Social✅*, store-item-tile✅*). Remaining: work through the 17 groups' components. Method is reliable + honest (source-read avoids false positives, DOM-metric handles video-blank). |
- 2026-08-11 — champion-card diffed INLINE-agent (lightweight-prod, completed in a good window, 9 tools/2.4min). Measured structurally+token-correct (160×244, blue-7 name bar, gold-1 uppercase name — all tokens correct). Agent caught a REF-MAPPING error: client-champion-overview.jpg = champion-DETAIL panel, not the card grid → corrected the scorecard mapping. Flagged (not filed) name fontWeight 400-vs-bold as a plausible delta lacking a grid-view ref to confirm — correct no-false-positive call. 4 client components now measured-clean (Navbar✅*, Social✅*, store-item-tile✅*, champion-card◐). Intermittent-window note: this diff completed where the prior champion-card attempt stalled — the loop's opportunistic retry works. Remaining collection components (champion-detail, stat-medallion, skin-card, etc.) continue per tick.
- 2026-08-11 — skin-card diffed (lightweight-prod, completed clean window) → rigorous element-by-element MATCH, DRY. Ref-mapping confirmed correct (client-collection-skins.jpg IS the skin grid). Owned border gold-3 + finials, unowned brightness-50 + LockBadge, hover tooltip blue-7/90 213×87, DDragon loading art, RP omitted on tile — all ✓/token-compliant. 5 client components measured-clean (Navbar✅*, Social✅*, store-item-tile✅*, champion-card◐, skin-card◐). Client pass converging component-by-component; opportunistic diffs landing reliably in good windows.
- 2026-08-12 — champion-pick-tile diffed (lightweight-prod, MATCH DRY). Ref correctly mapped. tile 80×91, portrait 64², border gold-5/hover gold-3/selected gold-2 + teal drop-shadow(blue-2), hex mastery badge blue-2 '6', name 12px grey-1, disabled grayscale+opacity-50, useId glow filter. 6 client components measured-clean (Navbar✅*, Social✅*, store-item-tile✅*, champion-card◐, skin-card◐, champion-pick-tile✅*).
- 2026-08-17 — Home group re-diffed (cdiff-home, lightweight-prod + DOM-METRIC video-safe method) vs client-current-home-2025-mf.png at 1280×720 → MATCH/DRY, 0 issues. Resolves the ⚠️ DIFF-LIMITED flag: the mandatory DOM-metric diff (getBoundingClientRect per region, single non-retried capture) measured all 4 assembled regions correct — nav band 1280×72, content 1056×630, home rail 230×536, splash bleed behind nav, skins strip 390×257, social rail 224×630 flush at 1280, no overflow. Home ⚠️→✅. Remaining non-⬜: Lobby ⚠️ (no assembled route — 404, needs a built lobby screen first). Groups Navbar/Social/Store/Home all ✅; Clash/TFT-hub ⬜ (never built). Next tick: Lobby is blocked on build; rotate to a re-verify or await new surface.
- 2026-08-17 — Lobby group opened for component-level diffing (assembled lobby route still absent — 404 — so diff the component showcases). lobby-player-card diffed (cdiff-lobby, lightweight-prod, showcase loaded cleanly, ref mapping CONFIRMED vs client-lobby-party.png + client-lobby-party-v11.png). 3 material deltas filed status:ready: #1004 profile icon must be circular ~120px round gold ring (ours square 96×96), #1005 summoner name must be ABOVE the icon (ours below), #1006 empty invite slot must be a ~120px circle with + glyph only (ours 180×260 dashed rect with "INVITE" label). #1004 + #1006 are unambiguous (circular icons/slots per real client). #1005 (name-above) is counterintuitive — FLAGGED for visual re-confirm at build-review against the party refs before merging (added a reviewer note on the issue); if the ref shows name-below, close #1005 invalid rather than reorder. Lobby stays ⚠️ pending these builds. Next lobby components to diff after these land: lobby-header, player-banner, lobby-progression-panel, game-mode-card, match-found-modal.
- 2026-08-17 — lobby-player-card fixes SHIPPED (PR #1007 → 1c47bfe): #1004 circular 120px gold-ring icon (was square 96×96), #1005 name reordered ABOVE icon (ref-confirmed via client-lobby-party.png — names sit at top of each tall card), #1006 empty slot → 120px circle with + glyph (was 180×260 dashed rect + "INVITE" label). Token-clean, both gates, profileIconUrl CDN asset kept. lobby-player-card component now ✅; Lobby GROUP stays ⚠️ until the rest are diffed (lobby-header, player-banner, lobby-progression-panel, game-mode-card, match-found-modal, queue-status, queue-type-list, party-status-panel). Next lobby tick: lobby-header.
- 2026-08-17 — lobby-header diffed (cdiff-lobby-header, lightweight-prod, ref CONFIRMED vs client-current-lobby-2025.png + client-lobby-subbar.jpg + client-lobby-party.png). 2 material deltas filed status:ready: #1008 (2025 header strip too tall — 49px py-2 → ~36px py-1.5) + #1009 (legacy SRWingsGlyph doesn't match the LoL ranked feathermark — flagged for a REAL CommunityDragon asset check before hand-drawing per fidelity-method). DRY within tolerance: 2025 breadcrumb 14px/1.4px/uppercase, middot separator, back chevron, mode gem, info/copy/stats icons, green toggle pair; legacy diamond separators, count chip, pill toggle. lobby-header now has open deltas (⚠️). Lobby group progress: lobby-player-card ✅ shipped, lobby-header ⚠️ (#1008/#1009). Next: player-banner.
- 2026-08-17 — lobby-header fixes SHIPPED (PR #1010 → 27cffad): #1008 2025 header strip py-2→py-1.5 (49px→~36px), #1009 legacy SRWingsGlyph → REAL CommunityDragon asset (ranked-mini-crests/silver.png, feathered wings + center gem, matches client-lobby-subbar.jpg — the fidelity-method real-asset-first directive paid off; the old hand-drawn chevrons were clearly wrong). Token-clean, both gates. lobby-header now ✅. Lobby group: lobby-player-card ✅, lobby-header ✅; remaining ⬜/undiffed: player-banner, lobby-progression-panel, game-mode-card, match-found-modal, queue-status, queue-type-list, party-status-panel. TECH-DEBT (low-pri, note not filed): both lobby-header.tsx AND player-banner.tsx inline the raw.communitydragon.org host as a module const — worth consolidating into a `rankedMiniCrestUrl`/`communityDragonUrl` @low/fixtures helper in a future sweep (CLAUDE.md prefers CDN via fixtures helpers). Next lobby diff: player-banner.
- 2026-08-18 — player-banner diffed (cdiff-player-banner, lightweight-prod, ref CONFIRMED vs client-lobby-solo.jpg — solo V11 heraldic banner: crown+name above, profile medallion, level pill at ring bottom, double-V pointed bottom, 4 empty grey circles). Mostly MATCH via DOM-metric: flag column 120×300, teammate flag 96×260, empty slot 90×90 double-ring, name gold-1 14px uppercase, CommunityDragon wing PNGs render. 1 material delta filed: #1013 level badge 28px too tall (should be ~12px) — root-caused to the badge wrapper div lacking leading-none/flex so it inherits Tailwind base line-height 24px (1+24+1+2border=28); fix = wrapper flex items-center justify-center → collapses to text+padding height. player-banner near-✅ (1 delta #1013). Lobby group: lobby-player-card ✅, lobby-header ✅, player-banner ⚠️(#1013). Next: lobby-progression-panel.
- 2026-08-18 — player-banner level badge SHIPPED (PR #1014 → 9d4f242): #1013 badge wrapper flex items-center justify-center collapses the inherited 24px line-height box → badge now 12px (was 28px), measured via getBoundingClientRect; text/color/position unchanged, +2/-2. player-banner now ✅. Lobby group: lobby-player-card ✅, lobby-header ✅, player-banner ✅; remaining undiffed: lobby-progression-panel, game-mode-card, match-found-modal, queue-status, queue-type-list, party-status-panel. Next lobby diff: lobby-progression-panel.
- 2026-08-18 — lobby-progression-panel diffed (cdiff-progression, lightweight-prod, ref CONFIRMED vs client-current-lobby-2025.png bottom-right — the Progression/Invited tab panel; other lobby refs show the friend sidebar, not this panel). Mostly MATCH by token: panel border gold-5, bg blue-7, active tab gold-1, underline gold-3, medallion gold-3, blue check disc (blue-6/blue-3/blue-2), objective grey-2, beaufort/spiegel fonts, tab strip + badge layout all correct. 1 material delta filed: #1015 mission title text-grey-1 (#a09b8c) → should be text-gold-2 (#c8aa6e) per the component's own JSDoc "Gold-cream" intent + the ref's warmer cream (one-line, lobby-progression-panel.tsx:308). progression-panel near-✅ (1 delta #1015). Lobby group: player-card ✅, header ✅, player-banner ✅, progression-panel ⚠️(#1015). Next: game-mode-card.
- 2026-08-18 — lobby-progression-panel mission title SHIPPED (PR #1016 → 355b8dc): #1015 text-grey-1 → text-gold-2 (one-class swap, +1/-1, only the mission title changed; objective body stays grey-2, medallion unchanged). progression-panel now ✅. Lobby group 4/9 ✅: player-card, header, player-banner, progression-panel. Remaining undiffed: game-mode-card, match-found-modal, queue-status, queue-type-list, party-status-panel. Next: game-mode-card.
- 2026-08-18 — game-mode-card diffed (cdiff-game-mode-card, lightweight-prod, ref CONFIRMED vs client-pvp-mode-select.jpg — 4 mode tiles SR/TT/ARAM/TFT with crests, names, selected-state info panel). 3 deltas filed: #1017 mode name text-2xl(24px)→~40-48px (text-4xl/5xl — FLAGGED for build-time 1280 overflow check + ref-scale caution), #1018 icon container h-32/w-32(128px)→~160px (h-40), #1019 STRUCTURAL: missing description + full-width gold-4 separator + queue-type bullet panel on selected state (needs new GameModeCardRow component + description/queueTypes props — entirely absent). game-mode-card ⚠️ (#1017/#1018/#1019). Lobby group 4/9 ✅ + game-mode-card in progress. Next after these build: match-found-modal.
- 2026-08-18 — game-mode-card fixes SHIPPED (PR #1020 → f9f2017): #1018 crest h-32→h-40 (160×160 measured), #1017 mode name text-2xl→text-4xl (overflow-gated: 4-tile row scrollWidth=826, body=1280, no overflow), #1019 selected-state info panel added as new GameModeCardRow component (gold-4 separator + description paragraph + ◈ queue-type bullets + warning triangle) with its own showcase + registry (alphabetical) + index export + .demo.tsx. Token-clean, both gates, 262 pages. game-mode-card now ✅ (+ new GameModeCardRow component). Lobby group 5/9 ✅: player-card, header, player-banner, progression-panel, game-mode-card. Remaining undiffed: match-found-modal, queue-status, queue-type-list, party-status-panel. Next: match-found-modal (video/glass — use DOM-metric).
- 2026-08-18 — match-found-modal diffed (cdiff-match-found, video/glass → screenshot BLANK, DOM-metric source diff used per method; ref CONFIRMED vs match-found-modal-clean-2.png + client-match-found-modal.png). Extensive MATCH: blind-pick glyph, MATCH FOUND! heading (font-display text-3xl gold-1 0.15em), subtitle, ACCEPT! button (200×44 rounded-6 navy + cyan inset + gold label), DECLINE tab, backdrop bg-hextech-black/70, outer bezel r=235, WAD video overlay wiring — all within tolerance. 1 material geometry delta filed: #1021 gold ring r=225 (inner edge 224.25) falls inside the CSS countdown arc's stroke band (center r=228, strokeWidth=9 → inner edge 223.5), so the arc paints over the gold ring; ref shows the gold ring as a distinct ring INSIDE the teal arc → move gold ring r=225→r=218 (outer edge 218.75, clear of arc). match-found-modal near-✅ (1 delta #1021). Lobby group 5/9 ✅ + match-found in progress. Next: queue-status.
- 2026-08-18 — match-found-modal gold ring SHIPPED (PR #1022 → 851b5dc): #1021 gold ring r=225→218 (outer edge 218.75 < arc inner edge 223.5, gap 4.75px) so it's a distinct ring inside the teal countdown arc; only the ring's r changed, arc/bezel/tokens untouched, +4/-2. match-found-modal now ✅. Lobby group 6/9 ✅: player-card, header, player-banner, progression-panel, game-mode-card, match-found-modal. Remaining undiffed: queue-status, queue-type-list, party-status-panel. Next: queue-status.
- 2026-08-18 — queue-status diffed (cdiff-queue-status, lightweight-prod, ref CONFIRMED vs client-queue-in-lobby.png — IN QUEUE strip: timer 0:27, IN QUEUE label, ESTIMATED 0:31, cancel). Mostly MATCH: pulse dot bg-blue-2, IN QUEUE label gold-2 tracking-widest, ESTIMATED grey-1, strip w-380 h-16 bg-blue-6 border-gold-5, teal glow shadow, over-estimate gold-3 state. 2 deltas filed (both queue-status.tsx): #1023 elapsed timer text-lg(18px)→text-2xl (ref timer ~22-24px cap, ~35-38% of 64px strip; panel layout already text-2xl), #1024 cancel affordance bare Unicode ✕ → styled ~24px circular button (h-6 w-6 rounded-full border-gold-5 + hover) in both strip+panel layouts. queue-status ⚠️ (#1023/#1024). Lobby group 6/9 ✅ + queue-status in progress. Next: queue-type-list.
- 2026-08-18 — queue-status fixes SHIPPED (PR #1025 → 47f7ac0): #1023 strip timer text-lg→text-2xl (matches panel), #1024 both strip+panel cancel affordances → circular ~24px button (h-6 w-6 rounded-full border-gold-5 flex, hover:border-gold-3 hover:text-gold-1) keeping ✕ glyph + callback. Token-clean, both gates, no overflow (strip stays w-380). queue-status now ✅. Lobby group 7/9 ✅: player-card, header, player-banner, progression-panel, game-mode-card, match-found-modal, queue-status. Remaining undiffed: queue-type-list, party-status-panel. Next: queue-type-list.
- 2026-08-18 — queue-type-list diffed (cdiff-queue-type-list, lightweight-prod, ref CONFIRMED vs client-pvp-mode-select.jpg — ◈ BLIND PICK selected + ◇▲ RANKED FLEX disabled+warning rows) → MATCH/DRY, 0 issues. FilledDiamond gold-3 ring + gold-1 core, OutlineDiamond currentColor, warning triangle --color-warning + grey-4 cutout, row gap-1, diamond→label gap-2.5, font-display uppercase tracking-widest text-sm, selected gold-1 bold + gold-3 diamond, disabled grey-2 cursor-not-allowed — all within tolerance. queue-type-list ✅ (DRY, no build). Lobby group 8/9 ✅: player-card, header, player-banner, progression-panel, game-mode-card, match-found-modal, queue-status, queue-type-list. LAST remaining: party-status-panel. Next: party-status-panel → then Lobby group review complete.
- 2026-08-18 — party-status-panel diffed (cdiff-party-status, lightweight-prod) → REF-UNAVAILABLE, 0 issues. No ref clearly shows the "OPEN PARTY" social-rail party widget (green band + silhouettes + crest chip + queue label): client-lobby-party.png/v11 are champ-select draft grids, client-current-lobby-2025.png party area too small to resolve, client-queue-in-lobby.png has no party indicator. Per the mis-mapped-ref precedent, filed nothing. Component renders correctly on prod: OPEN PARTY header + group icon, green band + crest chip + 5 filled/dimmed silhouettes, queue label caption, border-b border-gold-5 — tokens-only, no hardcoded colors, no 1280 overflow; internally sound (also selfcheck cy4 DRY). **LOBBY GROUP DIFF SWEEP COMPLETE (9/9): 8 confirmed 1:1 ✅ (player-card, header, player-banner, progression-panel, game-mode-card, match-found-modal, queue-status, queue-type-list) + party-status-panel REF-UNAVAILABLE-but-internally-sound.** Lobby group → effectively ✅ (all diffable components 1:1; the 9th has no reference to confirm against but is token-clean + selfcheck-DRY). This session shipped 6 lobby component fixes (11 issues #1004-#1024) + 1 new component (GameModeCardRow). Next client rotation: revisit the ⬜ never-built groups (Clash/TFT-hub are structural gaps needing BUILD, not diff) or re-verify an aging ✅ group.
- 2026-08-18 — skin-carousel diffed (cdiff-skin-carousel, lightweight-prod, ref CONFIRMED vs client-champ-select-loadout.jpg + client-loadout-skin-thumb-strip-detail.png — circular splash frame + skin name + dots + thumb strip). Mostly MATCH: SVG frame 308×308, skin name font-display italic 20px gold-1, active dot blue-2, inactive dot grey-3, selected thumb 118×66 gold-2 border + inset gold-4 ring, unselected 90×50, lock badge, ring art layers. 2 deltas filed (both skin-carousel.tsx): #1028 pagination dots 6px→8px, #1029 thumb strip missing dark semi-transparent tray bg behind thumbs+chevrons + chevron strokeWidth 2→2.5 (color-mix tokens, no raw rgba). skin-carousel ⚠️ (#1028/#1029). NOTE: reopened the champ-select/collection leaf-component ref-diff rotation (the 7 screen-groups were done, but many individual champ-select/collection components remain un-ref-diffed — lightweight-prod agents stay reliable while heavy worktree auditors die). Next: champion-detail or declare-intent-screen.
- 2026-08-18 — skin-carousel fixes SHIPPED (commit a444dc5, built INLINE in main): #1028 pagination dots 6→8px, #1029 dark semi-transparent thumb-strip tray (color-mix hextech-black 65%) behind BOTH strip rows (inline + standalone SkinThumbStrip) + chevron strokeWidth 2→2.5. Token-clean (no raw hex/rgba added; the rgba(0,0,0,0.6) on line 173 is a pre-existing black-shadow carve-out), both gates pass (typecheck 4/4 + web build). skin-carousel now ✅. **METHOD NOTE:** built inline in the main loop because 4 CONSECUTIVE heavy-worktree agents died to connection drops this window (2 selfcheck auditors + 2 skin-carousel builders) — the disposable-worktree builder pattern is unreliable in a degraded background-agent window; for TRIVIAL fixes the controller can edit @low/ui source inline + run both gates in main (reliable) rather than repeatedly orphaning lanes. Lightweight-prod DIFF agents stayed reliable throughout. Next champ-select leaf: champion-detail or declare-intent-screen (via lightweight-prod diff).
- 2026-08-18 — champion-detail diffed (cdiff-champion-detail, lightweight-prod, ref CONFIRMED vs client-champion-overview.jpg + client-champion-abilities.jpg + client-champion-mastery-tab.png). Mostly MATCH: 5-tab strip, mastery tab layout, overview info panel + stat wheel, active-ability border/glow, lore/clamp/owned-chip/learn-more all within tolerance. 2 deltas filed (both champion-detail.tsx): #1030 header champion icon uses hand-drawn CrestGlyph SVG → replace with REAL Data Dragon championSquareUrl portrait (~56px, border-gold-4) — championSquareUrl already in @low/fixtures, just never imported (REAL-ASSETS-over-SVG per fidelity-method); #1031 abilities-tab passive separator missing → 1px grey-3 vertical divider between P and Q icons. champion-detail ⚠️ (#1030/#1031). Collection leaf rotation progressing. Next: declare-intent-screen or emote-wheel. NOTE: heavy-worktree builders dying this window (4×) — if the #1030/#1031 builder dies, build inline in main (both are trivial single-file edits, gate + commit) as done for #1028/#1029.
- 2026-08-18 — champion-detail fixes SHIPPED (commit 73bbe6f, built INLINE in main): #1030 header CrestGlyph SVG → real Data Dragon championSquareUrl portrait (56px, border-gold-4 — REAL-ASSETS-over-SVG), #1031 abilities-tab passive separator (1px grey-3 self-stretch divider after the P slot, Fragment-wrapped map). Token-clean, both gates (typecheck 4/4 + web build). champion-detail now ✅. Client collection/champ-select leaf rotation: skin-carousel ✅, champion-detail ✅. Next leaf: declare-intent-screen or emote-wheel (via lightweight-prod diff). Heavy-worktree builders still degraded this window → trivial fixes continue inline in main.
- 2026-08-18 — emote-wheel diffed (cdiff-emote-wheel, lightweight-prod, ref CONFIRMED vs client-emote-wheel-frame-detail.png + client-collection-emotes.jpg). Mostly MATCH: junction bracket ornaments, halo ticks, double gold ring, center slot, labels-above-satellites, satellite circle ~90px all within tolerance. #421 central-frame approximation confirmed still within visual tolerance at 330px (not re-filed). 2 deltas filed (both emote-wheel.tsx): #1033 channel spokes too narrow — ChannelSpoke outerHalf 4.5→9 (9px→18px) + quadrantArcPath GAP 8°→14° to avoid arc-spoke overlap (MECHANICAL — 2 numbers); #1032 satellite slot positions wrong — currently symmetric 2×2 grid, ref shows asymmetric ORBIT (START ~8 o'clock lower-left, VICTORY ~1-2 o'clock upper-right, FIRST BLOOD + ACE side-by-side directly below wheel) → needs absolute per-satellite positioning with measured px offsets (MORE INVOLVED — build carefully, verify against ref crop, no 1280 overflow). emote-wheel ⚠️ (#1032/#1033). Leaf rotation: skin-carousel ✅, champion-detail ✅, emote-wheel ⚠️. Next: declare-intent-screen. NOTE: #1033 is inline-safe (2 numbers); #1032 needs careful positioning — prefer a builder if the heavy window recovers, else inline with a screenshot-vs-ref check.
- 2026-08-18 — emote-wheel fixes SHIPPED (PR #1034 → 3c04b2d, via builder — HEAVY WINDOW RECOVERED): #1033 channel spokes outerHalf 4.5→9 (18px) + quadrantArcPath GAP 8°→14°, #1032 satellite 2×2 grid → asymmetric ORBIT via absolute SAT_OFFSETS (start -307,+211 ~8o'clock; victory +307,+194 ~1-2o'clock; first-blood -114,+353 + ace +114,+353 below), no 1280 overflow (rightmost 1167-1230). Token-clean (only rgba(0,0,0,0.8) black-shadow carve-out added), both gates. emote-wheel now ✅. Leaf rotation: skin-carousel ✅, champion-detail ✅, emote-wheel ✅. **WINDOW NOTE: heavy-worktree builders RECOVERED — build-emote-wheel completed cleanly after 4 prior consecutive deaths; heavy builds/audits resume (with resilient report-early briefs).** Next leaf: declare-intent-screen.
- 2026-08-18 — declare-intent-screen diffed (cdiff-declare-intent, lightweight-prod, ref CONFIRMED vs client-champ-select-declare-intent.png + client-champ-select-fullscreen-declare.png + client-declare-intent-roster-compare.png; map video rendered, DOM-metric used). Mostly MATCH: medallion 59×59, active bar 8px, tray 92px, header height, gold tokens, chip bg blue-2 + font-display 12px all within tolerance. 3 deltas filed (all declare-intent-screen.tsx, mechanical): #1035 empty champion slot h-1.5(6px)→h-[14px] recognizable chip, #1036 roster sidebar width 220→270px (~21% of 1280 per fullscreen ref), #1037 self-row countdown chip should hug right edge (~4px not 16px gap — adjust pr). declare-intent-screen ⚠️ (#1035/#1036/#1037). Leaf rotation: skin-carousel ✅, champion-detail ✅, emote-wheel ✅, declare-intent-screen ⚠️. Next: emote-tile or stat-medallion. Heavy builder window RECOVERED (PR #1034 completed) — these 3 can go to a normal builder.
- 2026-08-19 — declare-intent-screen fixes SHIPPED (PR #1038 → 2c050ba, via builder): #1035 empty slot h-1.5→h-[14px], #1036 roster sidebar 220→270px (map stage flex absorbs, no overflow), #1037 self-row countdown chip pr-1→pr-0 (chip px-1 = ~4px right gap). Token-clean, both gates. declare-intent-screen now ✅. Leaf rotation: skin-carousel ✅, champion-detail ✅, emote-wheel ✅, declare-intent-screen ✅ (4 champ-select/collection leaf components converged this session). Next leaf: emote-tile, stat-medallion, or countdown-header.
- 2026-08-19 — countdown-header diffed (cdiff-countdown-header, lightweight-prod, ref CONFIRMED vs client-champ-select-pick.jpg + client-champ-select-fullscreen-declare.png). Mostly MATCH: filigree ornament (vs client-champ-select-header-ornament.png), title text-2xl tracking-widest gold-1, teal fill tokens (blue-4/blue-1 gradient, blue-6/60 track) all within tolerance. Correctly NOT filed: font weight 400 (Beaufort natural weight, not an override); showcase title "Loadout" vs "Champion" (fixture/demo data, not component). 2 deltas filed (both countdown-header.tsx): #1039 progress bar STRUCTURALLY wrong — currently two split flanking bars LEFT+RIGHT of the number (each 189px), ref shows a SINGLE full-width bar ABOVE the number (stacked) → restructure the header layout (build carefully, screenshot vs ref); #1040 bar track h-[3px]→~5px (mechanical). countdown-header ⚠️ (#1039/#1040). Leaf rotation: skin-carousel ✅, champion-detail ✅, emote-wheel ✅, declare-intent-screen ✅, countdown-header ⚠️. Next: emote-tile or stat-medallion.
- 2026-08-19 — countdown-header fixes SHIPPED (PR #1041 → 0284f87, via builder): #1039 progress bar restructured — two split flanking bars → single full-width CountdownBar (w-full) in a stacked flex-col (title → full-width bar → centered number), matching ref; #1040 track h-[3px]→h-[5px]. Token-clean (net -17 lines, simplified), both gates, no overflow. countdown-header now ✅. Champ-select/collection LEAF ROTATION this session: skin-carousel ✅, champion-detail ✅, emote-wheel ✅, declare-intent-screen ✅, countdown-header ✅ (5 components converged). Next leaf: emote-tile, stat-medallion, lock-in-button, team-player-row, ban-status-strip, skin-thumb-strip, or collection tabs (spells/stats/runes/challenges/skin-preview/mastery/rank overlays).
- 2026-08-19 — lock-in-button diffed (cdiff-lock-in-button, lightweight-prod, ref CONFIRMED vs client-champ-select-pick.jpg — teal trapezoid LOCK IN button bottom-right) → MATCH/DRY, 0 issues. All within tolerance: trapezoid geometry (CommunityDragon asset fit #427), teal gradient fill rgb(16,66,88)→rgb(15,189,214), near-white hot frame, two-stop glow, label font-display text-lg tracking-0.12em 18px, disabled (grey-4/grey-3/grey-2), ban variant (ban-red gradient), CrossfadeVideo state machines (FIND MATCH #310 + LOCK IN #428). lock-in-button ✅ (DRY, no build). Leaf rotation: skin-carousel ✅, champion-detail ✅, emote-wheel ✅, declare-intent-screen ✅, countdown-header ✅, lock-in-button ✅ (6 components; 5 fixed + 1 DRY this session). Next: team-player-row, ban-status-strip, skin-thumb-strip, or collection tabs (spells/stats/runes/challenges/skin-preview/mastery/rank overlays).
- 2026-08-19 — team-player-row diffed (cdiff-team-player-row, lightweight-prod, ref CONFIRMED vs client-champ-select-spell-rail.jpg — team rail: spells→portrait→text, order MATCHES). 2 deltas filed: #1042 portrait uses championSquareUrl (120² full-body square icon) but ref shows loading art (308×560 face-centered tall portrait) — swap showcase to loadingArtUrl (already in @low/fixtures) + JSDoc clarify expected asset; #1043 missing ~3px gold left-edge accent bar on the isSelf=true row (component only changes text colors, no bar) → add border-l-2 border-gold-3 when isSelf. Correctly NOT filed (within tolerance): picking-ring thickness (ring-2 + animate-pulse compensates), locked-name color (text-grey-1 matches token, ref slightly brighter but 1-stop). team-player-row ⚠️ (#1042/#1043). Leaf rotation: 6 ✅ + team-player-row ⚠️. Next: ban-status-strip, skin-thumb-strip, or collection tabs (spells/stats/runes/challenges/stat-medallion/emote-tile/skin-preview/overlays).
- 2026-08-19 — team-player-row fixes SHIPPED (PR #1044 → 5667844, via builder): #1042 showcase portraits championSquareUrl→loadingArtUrl (Ashe/Warwick/Kindred/Lux tall face-centered loading art) + portraitSrc JSDoc clarified, #1043 self/active row gold left accent (border-l-[3px] border-gold-3 pl-[1px] when isSelf, non-self unchanged, no overflow). Token-clean, both gates. team-player-row now ✅. Champ-select/collection LEAF ROTATION this session: skin-carousel ✅, champion-detail ✅, emote-wheel ✅, declare-intent-screen ✅, countdown-header ✅, lock-in-button ✅ (DRY), team-player-row ✅ = 7 components converged. Next: ban-status-strip, skin-thumb-strip, or collection tabs (spells-tab/stats-tab/runes-screen/challenges-screen/stat-medallion/emote-tile/skin-preview/honor+mastery+rank overlays/profile-ranked-screen).
- 2026-08-19 — runes-screen diffed (cdiff-runes-screen, lightweight-prod, ref CONFIRMED vs client-collection-runes.jpg + client-runes-toolbar-detail.png — rune page card browser: toolbar + 6 path filters + horizontal card grid). Mostly MATCH (extensive DRY): card 230×310, keystone icons 28px, art object-cover object-top, name uppercase display, hover rune-list overlay, all 6 path filter buttons (all-runes glyph + 5 DDragon path icons), trash pill rounded-full 58×32, counter tabular-nums, grid h-scroll, bookmark icon, no overflow. 1 delta filed: #1045 CREATE NEW double-frame button too tall (54px, pushes toolbar to 71px vs ~50px ref) → p-[3px]→p-[2px] outer + py-1.5→py-1 inner (~36px button, ~52px toolbar). runes-screen ⚠️ (#1045). Leaf rotation: 7 ✅ + runes-screen ⚠️. Next: spells-tab, stats-tab, stat-medallion, emote-tile, challenges-screen, skin-preview, ban-status-strip, skin-thumb-strip, or the overlays (honor/mastery/rank).
- 2026-08-19 — runes-screen CREATE NEW button height SHIPPED (commit 1811894, built INLINE): #1045 p-[3px]→p-[2px] outer + py-1.5→py-1 inner (toolbar 71px→~52px; search input py-1.5 untouched). Both gates. runes-screen now ✅. LEAF ROTATION this session: skin-carousel ✅, champion-detail ✅, emote-wheel ✅, declare-intent-screen ✅, countdown-header ✅, lock-in-button ✅, team-player-row ✅, runes-screen ✅ = 8 components converged. Next: spells-tab, stats-tab, stat-medallion, emote-tile, challenges-screen, skin-preview, ban-status-strip, skin-thumb-strip, profile-ranked-screen, or overlays (honor/mastery/rank).
- 2026-08-19 — challenges-screen diffed (cdiff-challenges-screen, lightweight-prod, ref CONFIRMED vs client-profile-challenges-tab.jpg — challenges tab: SILVER total, category sidebar, 5-col card grid). Structural MATCH: sidebar 205px, 5-col grid 118.6px/card, crystal SVG 64×72, tier badges, score chip, tooltip, truncated reward text (intentional). 3 deltas filed (all challenges-screen.tsx, more substantial): #1046 sidebar per-category score PROGRESS BARS missing (dot+text only; ref has ~3px h fill bar per category) → add categoryScores prop + h-[3px] bg-blue-5/blue-2 bars; #1047 content-header FILTER ROW missing (goes straight to grid; ref has ~36px Search input + Category dropdown) → add searchQuery/filterCategory presentational props (page owns logic); #1048 REAL-ASSETS — cards show generic hex-gem SVG placeholders; ref shows real per-challenge art → wire tokenIconSrc to challengeTokenUrl(id,tier) (helper exists in cdragon.ts, confirmed 200) using numeric CDragon config IDs from v1/challenges.json into SAMPLE_CHALLENGES. challenges-screen ⚠️ (#1046/#1047/#1048). Leaf rotation: 8 ✅ + challenges-screen ⚠️. #1048 needs CDragon config-ID lookup — prefer a builder. Next: spells-tab, stats-tab, stat-medallion, emote-tile, skin-preview, profile-ranked-screen, ban-status-strip, skin-thumb-strip, overlays.
- 2026-08-19 — challenges-screen fixes SHIPPED (PR #1049 → 483f571 + follow-up 205f9c9): #1046 sidebar per-category progress bars (categoryScores prop, h-[3px] bg-blue-6/60 track + bg-blue-2 fill), #1047 content-header filter row (Search input h-9 + Category select; searchQuery/filterCategory presentational props; stateful demo in .demo.tsx, showcase server-safe), #1048 REAL CDragon challenge token art (challengeTokenUrl wired in SAMPLE_CHALLENGES, raw.communitydragon.org added to next.config remotePatterns). challenges-screen now ✅. **REVIEW-OF-RECORD CATCH:** the builder confirmed the 10 CDragon config IDs return 200, but 3 of the specific id+TIER PAIRS used (102000/gold, 303600/diamond, 201100/silver) were 404 — those IDs have NO token art at ANY tier — so #1049 shipped 3 broken images (the exact defect #1048 was fixing). Caught by curl-testing every actual challengeTokenUrl(id,tier) output; replaced with verified master-tier combos (101100/101200/103000); all 10 now 200. LESSON: for CDN assets, verify the EXACT id+tier/variant pair the fixture uses, not just the base ID. Leaf rotation: 9 ✅ (skin-carousel, champion-detail, emote-wheel, declare-intent-screen, countdown-header, lock-in-button, team-player-row, runes-screen, challenges-screen). Next: spells-tab, stats-tab, stat-medallion, emote-tile, skin-preview, profile-ranked-screen, ban-status-strip, skin-thumb-strip, overlays.
- 2026-08-19 — profile-ranked-screen diffed (cdiff-profile-ranked, lightweight-prod, capture clean, ref CONFIRMED vs client-profile-ranked.jpg + client-profile-ranked-split.png + client-profile-ranked-tiles.png). 3 deltas filed (all profile-ranked-screen.tsx): #1050 feature tile height h-[150px]→~217px (tiles 261px wide, ref aspect ~0.83), #1051 queue selector pencil EditIcon → vertical sort/toggle arrows (↕ two stacked chevrons, SortArrowsIcon), #1052 current SP milestone label color text-gold-cream→text-blue-2 (ref shows "0/150 SP" teal/cyan). Correctly NOT filed (no precise source / ambiguous): Queue Up button width (full-client ref scaling), season badge icon (ambiguous at res), progress-track flanking chevrons (minor). profile-ranked-screen ⚠️ (#1050/#1051/#1052). Leaf rotation: 9 ✅ + profile-ranked-screen ⚠️. Next: spells-tab, stats-tab, stat-medallion, emote-tile, skin-preview, ban-status-strip, skin-thumb-strip, overlays (honor/mastery/rank-promotion).
- 2026-08-19 — profile-ranked-screen fixes SHIPPED (PR #1053 → c18b647, via builder): #1050 feature tile h-[150px]→h-[217px], #1051 pencil EditIcon → SortArrowsIcon (two ↑↓ chevrons; EditIcon fully removed, no orphan), #1052 current SP milestone text-gold-cream→text-blue-2. Token-clean, both gates, no overflow. profile-ranked-screen now ✅. LEAF ROTATION this session (10 components ✅): skin-carousel, champion-detail, emote-wheel, declare-intent-screen, countdown-header, lock-in-button, team-player-row, runes-screen, challenges-screen, profile-ranked-screen. Next: spells-tab, stats-tab, stat-medallion, emote-tile, skin-preview, ban-status-strip, skin-thumb-strip, honor-checkpoint-overlay, mastery-celebration-overlay, rank-promotion-overlay.
- 2026-08-19 — mastery-celebration-overlay diffed (cdiff-mastery-overlay, lightweight-prod) → REF-UNAVAILABLE, 0 issues. No reference exists for the mastery LEVEL-UP celebration overlay (crest + level + OK modal): client-champion-mastery-tab.png is the static mastery TAB (different surface), client-honor-checkpoint-celebration.png is a different overlay flow. Component source + showcase explicitly document "NO CHROME REFERENCE — four dry hunts (#370) — convention-based following honor-checkpoint-overlay (#365)"; crest + backdrop video are real client assets, surrounding frame is best-effort. Agent correctly filed nothing (mis-mapped-ref rule; verified candidate refs are wrong surface, no forced diff). mastery-celebration-overlay = REF-UNAVAILABLE-but-convention-sound (like party-status-panel). Leaf rotation: 10 ✅ + 1 ref-unavailable. Next: spells-tab, stats-tab, stat-medallion, emote-tile, skin-preview, ban-status-strip, skin-thumb-strip, rank-promotion-overlay, honor-checkpoint-overlay.
- 2026-08-19 — stats-tab diffed (cdiff-stats-tab, lightweight-prod, capture clean). REF-MAPPING CORRECTED: ref is client-profile-stats.jpg (profile stats tab — pentagon radar, Season 2019, Poro empty state, Summoner Search) — NOT client-champion-overview-statwheel.jpg (that's the champion-collection stat wheel, different surface; agent verified + rejected). MATCH: radar outer ring, spokes, diamond ticks, gold polygon fill, vertex role icons, season label, filter inputs, Poro mascot, Summoner Search all within tolerance. 2 deltas filed (both stats-tab.tsx): #1054 stats row layout — ref is 2-row (Games Played centered solo full-width row 1, Time Played + KDA Ratio side-by-side row 2, each framed by gold diamond-decorated dividers); prod is a single 3-col flex strip → restructure to 2 rows + dividers; #1055 PLAY STYLE heading — ref shows "PLAY"/"STYLE" two lines with sword icon flanking left + globe icon flanking right at heading level; prod is single centered "Play Style" (role icons only at SVG vertices) → two-line heading + flanking icons. stats-tab ⚠️ (#1054/#1055). Leaf rotation: 10 ✅ + 2 ref-unavailable + stats-tab ⚠️. Next: spells-tab, stat-medallion, emote-tile, skin-preview, ban-status-strip, skin-thumb-strip, rank-promotion-overlay, honor-checkpoint-overlay.
- 2026-08-19 — stats-tab fixes SHIPPED (PR #1056 → d8380ed, via builder): #1054 stats 3-col strip → 2-row layout (Games Played solo full-width row 1, Time Played + KDA Ratio row 2, new GoldDiamondDivider component matching client-profile-stats.jpg), #1055 "Play Style" → PLAY/STYLE two-line heading flanked by SwordIcon (left) + GlobeIcon (right). Token-clean, both gates, no overflow (280px sidebar unchanged). stats-tab now ✅. LEAF ROTATION this session (11 components ✅): skin-carousel, champion-detail, emote-wheel, declare-intent-screen, countdown-header, lock-in-button, team-player-row, runes-screen, challenges-screen, profile-ranked-screen, stats-tab (+2 ref-unavailable: party-status-panel, mastery-celebration-overlay). Next: spells-tab, stat-medallion, emote-tile, skin-preview, ban-status-strip, skin-thumb-strip, rank-promotion-overlay, honor-checkpoint-overlay.
- 2026-08-19 — ban-status-strip diffed (cdiff-ban-status-strip, lightweight-prod) → REF-UNAVAILABLE, 0 issues. Scanned ALL champ-select refs (client-champ-select-pick.jpg, declare-intent, fullscreen-declare, header-ornament, loadout, loadout-action-bar, spell-rail) — NONE show the ban phase strip with champion ban slots. Component is internally sound (5 ally + 5 enemy ban slots, grayscale portrait + dark overlay + red X on filled, gold glow on active, grey-4 dashed empty slots, "vs" divider) but no confirmed reference to diff against. Filed nothing (mis-mapped-ref rule). ban-status-strip = REF-UNAVAILABLE-but-internally-sound (3rd: party-status-panel, mastery-celebration-overlay, ban-status-strip). NOTE: the 80-ref set doesn't cover every component — the ban phase, celebration overlays, and party widget were built convention-based / without a direct client screenshot; these are selfcheck-swept + internally consistent but cannot be ref-confirmed 1:1. Leaf rotation: 11 ✅ + 3 ref-unavailable. Next: spells-tab, stat-medallion, emote-tile, skin-preview, skin-thumb-strip, rank-promotion-overlay, honor-checkpoint-overlay, champ-select-action-bar.
- 2026-08-19 — champ-select-action-bar diffed (cdiff-action-bar, lightweight-prod, capture clean, ref CONFIRMED vs client-champ-select-loadout-action-bar.jpg dedicated crop + client-champ-select-loadout.jpg). Extensive MATCH: bar height 56px h-14, rune dropdown 200×36 red-orange color-mix border, emote button 36², gold accent lines + vertical divider, layout order (left-line→glyph→dropdown→spells→divider→emote→right-line), real CommunityDragon spell icons (Flash/Ignite) all within tolerance. 2 deltas filed (both champ-select-action-bar.tsx): #1057 spell slots h-10 w-10 (40px)→h-11 w-11 (44px, ref slots ~44px in 56px bar); #1058 replace Unicode ✎ (U+270E) + OS emoji 🙂 with proper inline SVG icons (useId, currentColor text-gold-cream/hover gold-1) — no OS glyphs, real styled icons per method. champ-select-action-bar ⚠️ (#1057/#1058). Leaf rotation: 11 ✅ + 3 ref-unavailable + champ-select-action-bar ⚠️. Next: stat-medallion, emote-tile, skin-preview, skin-thumb-strip, spells-tab, rank-promotion-overlay, honor-checkpoint-overlay.
- 2026-08-19 — champ-select-action-bar fixes SHIPPED (PR #1059 → 3f7a011, via builder): #1057 spell slots h-10 w-10→h-11 w-11 (44px), #1058 Unicode ✎ → RuneNibIcon inline SVG + OS emoji 🙂 → SmileIcon inline SVG (both currentColor, useId for clip-path). Token-clean, both gates, no overflow (shrink-0 fixed elements + flex-1 right accent). Verified: NO Unicode pencil/emoji remain. champ-select-action-bar now ✅. LEAF ROTATION this session (12 components ✅): skin-carousel, champion-detail, emote-wheel, declare-intent-screen, countdown-header, lock-in-button, team-player-row, runes-screen, challenges-screen, profile-ranked-screen, stats-tab, champ-select-action-bar (+3 ref-unavailable: party-status-panel, mastery-celebration-overlay, ban-status-strip). Next: stat-medallion, emote-tile, skin-preview, skin-thumb-strip, spells-tab, rank-promotion-overlay, honor-checkpoint-overlay.
- 2026-08-19 — emote-tile diffed (cdiff-emote-tile, lightweight-prod, ref CONFIRMED vs client-collection-emotes.jpg — emote grid with CIRCULAR art tiles, NO text labels). MATCH: tile 80×80, art circle 56² rounded-full, border 1px grey-3→gold-2 select, hover gold-4, owned brightness-50, unowned opacity-80. 2 related deltas filed (both emote-tile.tsx): #1061 name label absent in real client — grid shows emote art ONLY (labels appear in detail panel on selection, not per-tile); current tile always renders a <span> label → breaks fidelity; fix = remove <span>, keep aria-label + img alt for a11y. #1060 content overflow — the label makes the inner flex column 86.5px inside the 80px tile (p-1.5×2 + h-14 + gap-1.5 + 12.5px label), overflow:visible bleeds 1.5px past bottom; removing the label (#1061) resolves this + add overflow-hidden. emote-tile ⚠️ (#1060/#1061 — same root fix). Leaf rotation: 12 ✅ + 3 ref-unavailable + emote-tile ⚠️. Next: stat-medallion, skin-preview, skin-thumb-strip, spells-tab, rank-promotion-overlay, honor-checkpoint-overlay.
- 2026-08-19 — emote-tile fix SHIPPED (commit e853fc4, built INLINE): #1060/#1061 removed per-tile name <span> (real client emote grid shows art only; names in detail panel on selection) → fixes the 1.5px content overflow + matches fidelity; art now centered (flex h-full items-center justify-center), overflow-hidden on root, name preserved via img alt + aria-label for a11y. Both gates. emote-tile now ✅. LEAF ROTATION this session (13 components ✅): skin-carousel, champion-detail, emote-wheel, declare-intent-screen, countdown-header, lock-in-button, team-player-row, runes-screen, challenges-screen, profile-ranked-screen, stats-tab, champ-select-action-bar, emote-tile (+3 ref-unavailable). Next: stat-medallion, skin-preview, skin-thumb-strip, spells-tab, rank-promotion-overlay, honor-checkpoint-overlay.
- 2026-08-19 — skin-preview diffed (cdiff-skin-preview, lightweight-prod, ref CONFIRMED vs client-skin-preview.jpg — Silver Kayle splash + name top-left + OWNED badge + thumbnail strip + chevrons + inspect/lore icons). MATCH: full-bleed splash, vignettes, bottom strip, chevrons, icon buttons, close button, layout all within tolerance. DRY (not present in ref for Silver Kayle): tier badge, chroma dots. 2 deltas filed (both skin-preview.tsx): #1062 skin name h2 fontWeight 400→font-bold (ref shows heavy ~600-700 letterforms), #1063 thumbnail strip missing chroma/count badge (ref shows a small "0" count badge at bottom of each thumb) → add optional chromaCount?: number to SkinThumbnail + token-colored badge. skin-preview ⚠️ (#1062/#1063). Leaf rotation: 13 ✅ + 3 ref-unavailable + skin-preview ⚠️. Next: stat-medallion, skin-thumb-strip, spells-tab, rank-promotion-overlay, honor-checkpoint-overlay.
- 2026-08-19 — skin-preview fixes SHIPPED (PR #1064 → 61c5b52, via builder): #1062 skin name h2 → font-bold (font-display font-bold text-3xl uppercase gold-1), #1063 ChromaCountBadge sub-component + chromaCount?: number on SkinThumbnail (14px pill bottom-right, demo values 0/2/3). Token-clean, both gates, no overflow, showcase server-safe (demo in .demo.tsx). skin-preview now ✅. LEAF ROTATION this session (14 components ✅): skin-carousel, champion-detail, emote-wheel, declare-intent-screen, countdown-header, lock-in-button, team-player-row, runes-screen, challenges-screen, profile-ranked-screen, stats-tab, champ-select-action-bar, emote-tile, skin-preview (+3 ref-unavailable: party-status-panel, mastery-celebration-overlay, ban-status-strip). Next: stat-medallion, skin-thumb-strip, spells-tab, rank-promotion-overlay, honor-checkpoint-overlay.
- 2026-08-19 — spells-tab diffed (cdiff-spells-tab, lightweight-prod, ref CONFIRMED vs client-collection-spells.jpg — SUMMONER spells grid, Teleport selected + detail panel; DISTINCT from champion abilities P/Q/W/E/R). MATCH: selected-cell gold-4 border, 300px left col, blurred right preview, 2px grid gap all within tolerance. 2 deltas filed (both spells-tab.tsx, trivial): #1065 spell icon renders ~49px vs declared 64px (4-col grid in 300px + p-2 constrains img) → add w-16 h-16 to the <img> in SpellCell; #1066 unlockLabel ("SUMMONER LEVEL 7") + modes ("Classic, Tutorial") use text-grey-2 (rgb 91,90,86 near-black low-contrast) → text-grey-1 (description already correctly grey-1). spells-tab ⚠️ (#1065/#1066). Leaf rotation: 14 ✅ + 3 ref-unavailable + spells-tab ⚠️. Next: stat-medallion, skin-thumb-strip, rank-promotion-overlay, honor-checkpoint-overlay.
- 2026-08-19 — spells-tab fixes SHIPPED (commit 56393b6, built INLINE): #1065 add w-16 h-16 to SpellCell <img> (renders declared 64px, grid cell was constraining to ~49px), #1066 unlockLabel + modes text-grey-2→text-grey-1 (match description contrast). Both gates. spells-tab now ✅. LEAF ROTATION this session (15 components ✅): skin-carousel, champion-detail, emote-wheel, declare-intent-screen, countdown-header, lock-in-button, team-player-row, runes-screen, challenges-screen, profile-ranked-screen, stats-tab, champ-select-action-bar, emote-tile, skin-preview, spells-tab (+3 ref-unavailable: party-status-panel, mastery-celebration-overlay, ban-status-strip). Next: stat-medallion, skin-thumb-strip, rank-promotion-overlay, honor-checkpoint-overlay.
- 2026-08-19 — honor-checkpoint-overlay diffed (cdiff-honor-overlay, lightweight-prod, ref CONFIRMED vs client-honor-checkpoint-celebration.png — CHECKPOINT REACHED: crest + title + subtitle + CTA; showcase clips fixed-overlay to 560×315 stage so DOM-metric used). MATCH: title/subtitle typography (30px font-display gold-cream 6.6px tracking), hairlines, layout. DRY (documented approximation): backdrop dark-forest radial vs real leaf/particle art (CDragon-unavailable per JSDoc). 2 deltas filed (honor-checkpoint-overlay.tsx + fixtures): #1067 CTA missing reward count — ref shows "OK +3" (text + green honor-orb icon + count); component renders plain "OK" → add reward?: number prop + orb icon + +{reward} using honor-green tokens; #1068 REAL-ASSETS — HonorCrestGlyph hand-drawn SVG (3 detached blades) diverges from ref's official CommunityDragon honor-crest medallion (continuous arch-wreath cartouche) → add honorCrestUrl(level) fixture helper + crestLevel prop (default 3), replace SVG with <img> in the 280×300 slot. FLAGGED #1068 for CDragon URL curl-verify (per #1048 broken-token lesson) + #1067 for honor-green token existence check. honor-checkpoint-overlay ⚠️ (#1067/#1068). Leaf rotation: 15 ✅ + 3 ref-unavailable + honor-checkpoint-overlay ⚠️. Next: stat-medallion, skin-thumb-strip, rank-promotion-overlay.
- 2026-08-19 — honor-checkpoint-overlay fixes SHIPPED (PR #1069 → 05deb46, via builder): #1067 CTA "OK +3" — reward?: number prop, HonorOrbIcon (filled circle + glint in --color-honor-green) + +{reward}, showcase reward={3}; #1068 crest reworked from 3 detached blades → unified continuous arch-wreath medallion (rounded shield outline + mirrored leaf-pair bezier lobes + crown/center gems + base bar, one connected shape), documented best-effort hand-drawn. Token-clean, both gates, no overflow, showcase server-safe. honor-checkpoint-overlay now ✅. **PROACTIVE CATCH:** pre-probed the CDragon honor-crest asset (9 paths + listing, all 404) BEFORE building → re-scoped #1068 from a real <img> to a hand-drawn improvement, avoiding a broken-image regression (same class as #1048, caught up-front this time). LEAF ROTATION this session (16 components ✅): + honor-checkpoint-overlay (+3 ref-unavailable). Next: stat-medallion, skin-thumb-strip, rank-promotion-overlay.
