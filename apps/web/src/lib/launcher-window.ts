/**
 * Fixed launcher dimensions — the Riot/League launcher renders as a bounded
 * desktop window (16:9), like the 1280×720 client (see client-window.ts).
 * The /launcher layout centers this fixed-size frame on a dark backdrop rather
 * than filling the browser viewport edge-to-edge.
 */
export const LAUNCHER_WIDTH = 1280;
export const LAUNCHER_HEIGHT = 720;
